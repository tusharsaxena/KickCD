# Decisions (KickCD)

- **C1 `O.NavRail`:** Adopted by NR-KC-02 (KickCD#33): the Grid page draws the rail, with Icons, Cast bar and Text Label as its entries under the Unit band.
- The library-absent stub gains `NavRail` (or, where the host keeps a hand-typed library list, that list gains `OptionsNav`), so the surface-parity case stays green.
- Plan: `Ka0sAddonsCommonTasks/docs/2026-09-26-NAVRAIL_ADOPTION/`.
