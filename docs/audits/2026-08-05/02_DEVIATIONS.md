# KickCD — Deviations (2026-08-05)

**Addon:** Ka0s KickCD 1.2.1 · **Standard:** **v2.21.0 (2026-08-04)** · **Prefix:** `KCD-`

**ID continuity.** `KCD-01`..`KCD-29` are closed (verified in the 2026-08-04 run). Of
`KCD-30`..`KCD-39`, **eight recur unchanged and keep their IDs**; `KCD-36` (stale `[tests]` badge) is
**closed** — `README.md:7` now reads `737%2F737`, matching today's run and `docs/test-cases.md`;
`KCD-39` (`docs/complexity.md`) is **withdrawn by the standard** — v2.19.0 retired that file, the
addon does not carry one, and carrying one would now itself be the finding. New IDs run
`KCD-40`..`KCD-49`.

**Evidence.** Every row below is backed by `file:line` in `03_EVIDENCE.md`, and every mechanical
check is recorded there with its real command and output.

---

## MUST

| ID | Section(s) | Sev | Deviation | Fix direction |
|---|---|---|---|---|
| **KCD-30** | `testing-§1`, `testing-§9`, AP #47 | MUST | **The shared test kit is vendored but not consumed.** `tests/_kit/` is byte-pinned by `tests/test_vendor_sync.lua` and loaded by nothing. `tests/run.lua:19-83` hand-rolls the case registry, the assertion set and the `--list` renderer; `tests/loader.lua` is a private sandboxed source loader; `tests/wow_mock.lua` (1066 LOC) is a full replacement whose own comment at `:573` calls its widget builder "a VERBATIM port of `tests/_kit/mock_base.lua`'s builder". `testing-§1` makes all four a MUST NOT. Compounding it, `tests/run.lua:39-55` **executes each case body at registration time** and short-circuits in list mode — the exact shape `testing-§1`'s *"Registration is not execution"* rule forbids, and the reason `--list` can in principle disagree with the run. `testing-§9` additionally requires the own-file list to come from `Loader.tocFiles`. | `Kit = dofile("tests/_kit/framework.lua")`; `Kit.expose(_G.KICKCD_TEST)`; `Kit.run{ dir = "tests/", suites = {...} }`. Delete the private framework block and `tests/loader.lua`. Rebuild `tests/wow_mock.lua` as `local base = dofile("tests/_kit/mock_base.lua")` plus this addon's per-key overwrites. Assertion names and semantics already match (`tests/run.lua:73-83`), so no suite file changes. Re-derive the TOC list through `Loader.tocFiles("KickCD.toc")`, keeping the explicit `LibKa0s.xml` file list and the case that pins it. |
| **KCD-31** | `options-ui-§1`, AP #47 | MUST | **Host copies of library-provided Options members installed onto the library instance**, so the library's own widget makers run KickCD's code: `Helpers.LSMValues` (`settings/Panel.lua:281`), `Helpers.AttachTooltip` (`:341`), `Helpers.EnsureScroll` (`:394`), `Helpers.AddSpacer` (`:443`). The library supplies all four (`libs/LibKa0s/Options.lua:322,502`; `libs/LibKa0s/OptionsWidgets.lua:333,362`) and its makers reach them **through the instance**. `LSMValues` is also a live contract difference: the host's returns a **table**, the library's returns a **closure** — and `options-ui-§6` requires the deferred closure. | Delete the four host implementations and their `Helpers.*` assignments. Sweep the `LSMValues` call sites — each is `values = function() return H.LSMValues(m) end` (`settings/Icons.lua:196,220`; `settings/Label.lua:147`; `settings/Castbar.lua:280,401,438,463,500`) and becomes `values = H.LSMValues(m)`. Note the empty-media fallback key moves from `"Default"` to the library's `LSM_NONE`. `Helpers.AnchorValues`/`AnchorOrder` are genuinely host-shaped and stay. |
| **KCD-32** | `options-ui-§8`, AP #47 | MUST | **Host copies of the library's layout constants.** `core/Constants.lua` declares `PANEL_PADDING_X = 16`, restating `lib.LAYOUT.PADDING_X` (`libs/LibKa0s/Options.lua:46`), read at `settings/Panel.lua:307`; `settings/Panel.lua:426` declares `local ROW_VSPACER = 8`, restating `Options.lua:56`, and `:430` then **overwrites the library's published `O.ROW_VSPACER`** (`Options.lua:176`) with it. The file's own comment at `:300-306` cites the rule and breaks it twenty lines later. | Delete `local ROW_VSPACER = 8` and the `Helpers.ROW_VSPACER =` assignment; read `Helpers.ROW_VSPACER` at the call sites (`settings/Panel_Render.lua:20` already does). For `PADDING_X` the library does not publish it on the instance today, so the compliant route is an **additive** `O.PADDING_X` pushed upstream to `LibKa0s-Options-1.0`, re-vendored, then read off the instance — never a local edit under `libs/`. Until that minor lands, keep `Const.PANEL_PADDING_X` and record it in the file as a time-boxed deviation naming this ID. |
| **KCD-33** | `events-frames-taint-§8`, AP #35, AP #47 | MUST | **A second secret-safe stringifier** lives in `core/Compat.lua:373-386` — a private `safeRender` inside `Compat.DebugInterrupt`. Two rules bite: (a) the stringifier is `LibKa0s-Core-1.0`'s and an addon "MUST NOT hand-roll either" — the only sanctioned second copy is `core/CoreSetup.lua`'s library-absent branch, which already exists (`:76-98`); (b) its detection is `_G.issecretvalue` (`:374`), not the mandated `table.concat` probe, so the guard in the command whose whole purpose is diagnosing secrets is the one guard not built the mandated way. Adjacent: `core/Compat.lua:446` falls back to the global `print()`, which §8 forbids at a call site. | Delete `safeRender`; route every value through `NS.SafeToString` (published at `core/CoreSetup.lua:131`). Keep the `%q` quoting for plain strings by wrapping the shared result, not by re-deriving safety. Drop the `_G.print` fallback at `:446`. Keep the `issecretvalue` **reporting** column at `:387` — reporting whether a value is secret is the command's payload; it must simply stop being the safety mechanism. |
| **KCD-34** | `performance-§9`, `performance-§2`, `testing-§7` | MUST | **`tests/perf.lua` does not exist.** `performance-§9` makes the offline scenario runner a MUST at that exact path, outside the green gate, asserting only deterministic quantities; it further requires a **zero-overhead scenario** pinning that the hottest bracketed path with capture off allocates no more than the same path with instrumentation absent. `performance-§2` names that scenario the **required evidence** for the gated-bracket rule. Today it is a comment. The addon's own record says so: `docs/automated-tests/RESULTS.md:61-66`. Newly sharper under v2.21.0 — see `KCD-44`. | Add `tests/perf.lua`: derive its addon-file list through the kit's `Loader.tocFiles` (`testing-§9`), load the addon under the mock, and ship at minimum the zero-overhead scenario over `iconApply` — `core/PerfSetup.lua:19-31` already names it as the addon's only real hot path — measuring `collectgarbage("count")` deltas with a full collect either side, capture off vs. `NS.Perf` stubbed absent. Assert allocation and call counts only, never wall clock. Keep it out of `tests/run.lua`; pin the derivation call by reading its source from a gated suite. |
| **KCD-35** | `documentation-§3`, `performance-§8` | MUST | **Two required topic-detail docs are absent:** `docs/performance.md` and `docs/perf-runs/README.md`. `documentation-§3` names five required topic-detail docs; three exist. `performance-§8` additionally requires `docs/perf-runs/` to be the standing, cumulative **in-game** capture store with a README documenting the naming convention, a schema summary, a pointer to the library's canonical record contract, and the note that offline runs live in `docs/automated-tests/`. | Write `docs/performance.md`: which paths are bracketed and why (the reasoning is already written, in `core/PerfSetup.lua:19-31` and `:96-122` — move it, do not duplicate it), how to run `/kcd perf`, how to read the report, and what the harness can and cannot resolve; point at the library for the shared protocol. Create `docs/perf-runs/README.md` with the `<YYYY-MM-DD>-ingame-<label>.json` convention, a schema summary, the upstream pointer, and the `automated-tests-§7` split. Commit the first capture worth keeping alongside it. |
| **KCD-37** | `documentation-§1` | MUST | **An angle-bracket placeholder in shipped README content:** `README.md:191` renders `` `/kcd reset <setting>` `` in the Troubleshooting table. `documentation-§1` is explicit that CurseForge strips `<setting>` as unknown HTML **including inside backticks**, so the line reaches players as `/kcd reset` with the placeholder silently deleted while looking correct on GitHub. The rest of the README already uses the bare form (`:73-75`), so this is one straggler. | Change to `` `/kcd reset setting` `` to match `README.md:75`. The `<br>` tags in the Version History cells (`:210-211`) are deliberate HTML and **MUST NOT** be swept — `documentation-§1` protects them by name. |
| **KCD-38** | `architecture-§4`, AP #17 | MUST | **Six sends of one bus message from five files.** `Ka0s_KickCD_CONFIG_CHANGED` is sent from `core/KickCD.lua` (×2), `modules/Castbar.lua`, `modules/IconGrid.lua`, `settings/Spells.lua` and `settings/Panel.lua`. `architecture-§4`: "MUST NOT have two senders for the same message" (AP #17), and each message is documented with **one** sender. It is recorded honestly in-repo (`docs/ARCHITECTURE.md:118` lists five senders), which makes it a *known* deviation rather than an unknown one — but the standard's escalation path is that the user classifies it as an accepted deviation or changes the standard, and until one of those happens the audit records it. | A design pass, not an edit. Two viable ends: (a) narrow to a single owning module — the natural owner is `settings/Panel.lua`'s `Helpers.FireConfigChanged` (`:75`), with the other five call sites routing through it; or (b) take it upstream as a standard change carving out a settings-changed fan-in shape. Do not leave it in the middle: `docs/message-bus.md`'s own rule (a second emitter must live in the same file as the first) is already broken by the current five files. |
| **KCD-40** | `options-ui-§5`, `options-ui-§1`, AP #47 | MUST | **A private page registry and category bootstrap run alongside the adopted library's.** `settings/Panel.lua:546` declares `NS.Settings.RegisterTab`; `:554-604` declares a private `RegisterPanel` that calls `Settings.RegisterCanvasLayoutCategory` itself (`:589`) and drives the page builders (`:594-603`); a private bootstrap frame at `:607-616` calls it. All six pages register through `NS.Settings.RegisterTab`, not through the library's `RegisterOptionsPage`. `options-ui-§5` makes both a MUST the other way: "Each page **MUST** register itself through the library's page registry (`RegisterOptionsPage(key, name, builder)`)", and the parent category is registered by "the library's `CreateOptionsPanel`… the host **MUST** call it once at `PLAYER_LOGIN`". The library's `RegisterOptionsPage`/`CreateOptionsPanel`/`OpenOptionsPanel` are wired at `settings/OptionsSetup.lua:228-230` and have **no production callers** — `NS.OpenOptionsPanel` is reached only from `tests/test_coresetup.lua:354` and `tests/test_options_panel.lua:342`; `NS.RegisterOptionsPage` and `NS.RefreshOptionsPanel` have none at all. `core/KickCD.lua:775-790` is a second panel-open path around the library's (it *is* combat-gated at `:735-739`, so `options-ui-§2` is satisfied, but it is still the second one). | Move each page's `NS.Settings.RegisterTab("<key>", Build)` tail (`settings/General.lua:183`, `Icons.lua:417`, `Castbar.lua:560`, `Label.lua:193`, `Spells.lua:1158`, `Profiles.lua:66`) onto `NS.RegisterOptionsPage(key, name, Build)`; delete `NS.Settings.RegisterTab` and `RegisterPanel`, and call `NS.CreateOptionsPanel()` once from the existing `PLAYER_LOGIN`/`ADDON_LOADED` bootstrap. Repoint `NS:OpenSettings` at `NS.OpenOptionsPanel` and keep only the host-specific retry/expand behavior around it, or push that behavior upstream as descriptor seams. Sequence **after** `KCD-31`/`KCD-32`, which touch the same file. Landing this closes the largest remaining `#47` surface in the addon. |
| **KCD-41** | `savedvariables-§2` | MUST | **Profile defaults are not in `defaults/Profile.lua`.** `savedvariables-§2`: defaults "**MUST** declare in `defaults/Profile.lua`" and that "**MUST** be the **only** place a default value is hardcoded". `DEFAULT_PROFILE` lives at `core/Database.lua:239-303`, and `defaults/` holds only `defaults/Spells.lua`. A **second** hardcoded copy of the same defaults then exists at `modules/Castbar.lua:566-590` — and it disagrees with the first about the color shape, which is what `KCD-42` is. | Move `DEFAULT_PROFILE` into `defaults/Profile.lua`, published as `NS.C` / `NS.DEFAULT_PROFILE`; add the file to the TOC's `# Defaults` block ahead of `defaults\Spells.lua`; leave `core/Database.lua` owning only the AceDB assembly and the migration runner. Then delete the `modules/Castbar.lua` fallback table and read the shared one. `tests/test_database.lua` already pins the tree's shape, so the move is characterized before it starts. |
| **KCD-42** | `options-ui-§1`, `testing-§4` | MUST | **A stored-color contract violation with a user-visible effect.** Colors have been stored keyed `{r=,g=,b=,a=}` since the v3→v4 migration (`core/Database.lua:203`, `:213`; schema rows at `settings/Castbar.lua:419`, `:481`), and every other read goes through `NS.Util.Unpack`, which handles both shapes (`core/Util.lua:22-28`). But `modules/Castbar.lua:726-729`'s `rgba()` reads **positionally only**, so `intCfg.nameTextColor.r` is invisible to it and the fallback `WHITE` wins on every call — the cast bar's **Spell name color** setting always paints white (`:740`, `:747-748`). `options-ui-§1` makes the stored shape "a real contract with the rest of the addon"; `testing-§4` requires a covering test for the behavior, and `nameTextColor`/`rgba` appear in no file under `tests/`. | Replace `rgba`'s body with `NS.Util.Unpack(c or WHITE)` — the signature and the allocation-free return are unchanged, and `Unpack` already defaults each channel. Add a case to `tests/test_color_shape.lua` driving `applyPreviewVisuals`/`applyLiveVisuals` with a keyed non-white color and asserting the value that reaches `SetTextColor`; run it red against the current code first (`testing-§13`). While there, delete the positional `nameTextColor` defaults at `modules/Castbar.lua:573`/`:583` under `KCD-41`. |
| **KCD-43** | `testing-§12`, `testing-§11` | MUST | **Two tests that pass without looking.** `tests/test_vendor_sync.lua:138-142` and `:144-149` both call `siblingTag()` (`:110-116`) and `return` before asserting when `git -C ../LibKa0s show` answers nothing — then print `PASS` and count toward the 737 and the README badge. The case names are unconditional positive claims ("libs/LibKa0s **is** the LibKa0s release…"). `testing-§12`: a test that passes no matter what the implementation does "reads as coverage while providing none", which is "strictly worse than an absent test"; `testing-§11` states the rule for exactly this gate — it "**MUST** fail, not pass, when the gate **cannot run**". The comment at `:107-109` claims the skip "is said in the case name"; it is not. This is not hypothetical: today's run printed PASS for both, and the sibling repo was never read. | Make the not-run state visible. Either (a) fail when the sibling is absent, per `testing-§11`, or (b) if a genuinely optional local checkout is wanted, register a *different* case name when the sibling is missing (`"vendor sync SKIPPED — ../LibKa0s not checked out"`) so the inventory and the badge cannot report an unmeasured gate as a passing one. Fix the `:107-109` comment either way. Do not leave the current shape, whose only failure mode is a checkout that happens to exist. |
| **KCD-44** | `automated-tests-§3` (*The release gate*), `documentation-§5` | MUST | **The release gate is unstated, and the addon cannot currently pass it.** v2.21.0 added a second checkpoint: a release **MUST NOT** be cut unless the release run's `manifest.json` shows **all four** suites at `pass` and `suites.complexity.warnings == 0`, and a **skip is a gate that did not pass**. Both of the addon's own statements stop at the commit gate and read as though perf and complexity never gate at all: `docs/testing.md:91-100` and `CLAUDE.md:75` ("Complexity — recorded, never a gate"). Meanwhile the newest run's manifest records `"perf": {"status": "skip"}` (`docs/automated-tests/20260804-233245/manifest.json`) — which falls under the section's one narrow exception (the addon ships no `tests/perf.lua`) and therefore **MUST be stated as such in the release notes**, something no release note does. | Two doc edits and one dependency. In `docs/testing.md`, add the release gate beside the commit gate: all four at `pass` plus zero CCN > 15 at the tag, evaluated by `/wow-addon:bump-version` from the manifest, never by the runner's exit code; and state that a `skip` blocks as NOT EVALUATED. In `CLAUDE.md:75`, qualify "never a gate" with "at **commit**" and name the release gate. Then either land `KCD-34` (which turns `perf: skip` into a real `pass`) or write the narrow exception into the release notes each time. `docs/automated-tests/README.md` should carry the same two-checkpoint statement. |
| **KCD-45** | `debug-logging-§7`, `debug-logging-§3` | MUST | **The DebugLog stub re-implements the library's line format.** `core/DebugLogSetup.lua:94-96` defines `FormatPlain(ts, tag, msg)` returning `<ts> | [<tag>] <msg>`, and `:114` aliases `FormatColored` to it. `debug-logging-§7`: "The stub **MUST NOT** re-implement the formatters or the line format (§3)"; §3 adds that the addon "**MUST NOT** redefine, wrap, or hand-copy those formatters". The comment at `:58-62` argues the colors are not reproduced — true, and it is a written-down decision — but the rule names the **line format** as well as the colors, and this is a byte-for-byte copy of it. | Delete both. If the suites need a formatter shaped answer from the stub, return the message alone (or `nil`) and let the case assert on the library path where the format is actually specified. Keep the comment, rewritten to say what was removed and why — the next author will otherwise re-add it for the same reason. |
| **KCD-46** | `events-frames-taint-§8`, `debug-logging-§4`, `debug-logging-§12` | MUST | **The castbar dump bypasses the shared seam three ways.** `modules/Castbar_Debug.lua:125` binds `NS.Util.print or _G.print` — §8 forbids the global `print()` at a call site outright. All 20 dump lines build their message with `..` before handing it over (`:35`, `:60`, `:82`, `:85`, `:102`, `:114`, `:126`, …) — §8: call sites "**MUST NOT** … feed chat/debug args through `..` / `tostring` / `table.concat` before the shared printer", because pre-concatenation is exactly what defeats the per-argument secret guard, in the one file whose whole subject is possibly-secret values. And the dump lands in **chat** rather than the console — `debug-logging-§12` routes an explicit user-initiated diagnostic run through the ungated raw append (`NS.DebugLog:Add`), which is already reachable and already used that way by `core/PerfSetup.lua:169-170`. | Drop the `_G.print` fallback. Pass the pieces as varargs (`print("  current.isChannel = ", current.isChannel)`) so the printer stringifies each one. Route the dump through `NS.DebugLog:Add("Cast", line)` — ungated, per §12 — and reveal the console once at the start of the dump rather than per line. `tests/test_castbar_debug.lua` already drives this path and will characterize the change. |
| **KCD-47** | `localization-§5`, AP #46 | MUST | **British spellings in authored docs prose.** `docs/superpowers/plans/2026-08-04-ccn-elimination.md` uses `colours`/`colour` at `:51`, `:53`, `:57`, `:121`, `:149` and `behaviour` at `:51`. `localization-§5` puts "prose in `README.md` and every file under `docs/`" squarely inside the US-English MUST, and AP #46 names it. None of the sanctioned exceptions applies — these are the addon's own words about its own code. | `colour` → `color`, `colours` → `colors`, `behaviour` → `behavior` in that file. **Do not** touch `docs/automated-tests/20260804-182144/ANALYSIS.md:72` (`behaviour`): a run bundle is frozen evidence (`automated-tests-§1`) and editing it is worse than the typo. Note it in the next `ANALYSIS.md` instead, and add the spelling sweep to the pre-release doc pass so the next plan file is written right. |

## SHOULD

| ID | Section(s) | Sev | Deviation | Fix direction |
|---|---|---|---|---|
| **KCD-48** | `options-ui-§1` (single write seam), `slash-commands-§3` | SHOULD | **A direct SavedVariables write outside the write seam, unguarded.** `settings/Slash.lua:103-132`'s `GateHint` resolves the gating row to `(parent, key)` and assigns `parent[key] = candidate` at `:120`, restoring it at `:122`. It is a deliberate transient probe and the comment at `:98-102` says so — but the probe between the two writes is `allowedKeys(row)`, which calls the row's `values()`; if that raises, the restore never runs and the user's gate setting stays changed in SavedVariables, silently, with no `onChange` and no panel refresh. Both sections require mutations to go through the addon's single write seam precisely so no path can diverge from it. | Wrap the mutate/probe/restore in `pcall` so the restore is unconditional, and add a case that makes `values()` raise and asserts the stored value is unchanged (`testing-§12`: mutate the implementation and watch it go red first). If the probe can instead be answered without mutating — by passing the candidate into `allowedKeys` — do that; a hint that needs no write is strictly better than one that needs a `pcall`. |
| **KCD-49** | `performance-§3`, `testing-§8` | SHOULD | **Two brackets are not closed on their early-return paths, so two buckets under-count.** `modules/IconGrid_Render.lua:826` opens the `cdText` bracket and `:831-837` returns without closing it; `modules/Castbar.lua:690` opens `castTick` and `:694-697` returns without closing it. `performance-§3` requires the report's totals to mean what they say, and this addon has already been bitten by exactly this — `core/PerfSetup.lua:107-113` records 73.9 ms attributed to nothing because of an unclosed multi-exit path. `tests/test_perfsetup.lua:551-589` pins the rule for `PollSpell` **only** (`testing-§8`: every declared bucket reached by a real bracket). Adjacent: `core/PerfSetup.lua:108` says "All four of PollSpell's exits", where `modules/Cooldowns.lua:179` says two and there are two (`:194`, `:202`). | Close both brackets on the early-return arm, the way `Cooldowns:PollSpell` already does (`modules/Cooldowns.lua:194`). Generalize `tests/test_perfsetup.lua`'s exit-coverage case from `PollSpell` to every bracketed function, so a new early return cannot silently reopen this. Correct the "four" at `core/PerfSetup.lua:108` to "both" in the same change. |

## Advisory (recorded, not deviations)

| ID | Section(s) | Note |
|---|---|---|
| **A-1** | `library-stack-§1` vs `-§3` | AceTimer-3.0 is not vendored; the addon schedules through `C_Timer.After` and never calls `LibStub("AceTimer-3.0")`. §3's "vendor only libs the addon actually `LibStub`" and §1's mandatory table cannot both be satisfied. Still a standard-internal tension, not an addon defect. Raise upstream if it recurs across the collection. |
| **A-2** | `layout-§1` | Four files in the 1000–1500 on-notice band: `modules/Castbar.lua` 1314, `settings/Spells.lua` 1172, `modules/IconGrid.lua` 1154, `tests/wow_mock.lua` 1066. `layout-§1` carries a MUST at 1500 and the words "on notice" for the band, with no SHOULD verb attached. All four carry live dispositions in `docs/automated-tests/RESULTS.md:138-144`, three under this ID and `wow_mock.lua` under `KCD-30`. The band grew on `20260804-214315` because the CCN work traded function complexity for file size — recorded, not hidden. Keep watching. |
| **A-3** | `packaging` | `.superpowers/` (54 files) and `.claude/settings.local.json` are dev-only and absent from `.pkgmeta`'s ignore list. The section's MUST names `docs/`, `_dev/`, `tests/` and lockfiles, all covered. Not a deviation; two ignore lines would make the package unambiguously clean. |
| **A-4** | `documentation-§1` | The extra `## Libraries and credits` section (`README.md:193-200`) sits between Troubleshooting and Issues. The canonical sections' **relative order** is preserved, which is what §1 requires, and the section is player-useful. Not a deviation. |
| **A-5** | `documentation-§2` | `CLAUDE.md`'s extra `## The docs/ set — there is no agent-context.md` section (`:19`) sits between the Standards-compliance section and the pointer list. The five required items are all present in order around it. Not a deviation. |
| **A-6** | `documentation-§4` | `docs/pending/LEDGER.md` is a decision ledger (done / wont-do / deferred), not a `TODO.md`, and §4's prohibition names `TODO.md` specifically. Not a deviation — but its `deferred` rows are functionally a second place work can hide from GitHub issues. Keep it short. |
| **A-7** | `toc-file-§1` | `KickCD.toc:14` keeps a commented-out `X-Wago-ID` placeholder with a `TODO(KCD-18)` marker. `X-Wago-ID` is a MAY and a commented line is an omitted field, so field order and the no-blank-lines rule both hold. Delete or fill it whenever Wago publication is decided. |
| **A-8** | `options-ui-§11` | `settings/Panel_Render.lua:259-266`'s `ResetAll` re-runs the work `afterRestoreAll` already does, so `CONFIG_CHANGED` fires twice each for general/castbar/units (also `docs/reviews/2026-08-05/01_FINDINGS.md` F-004). §11 fixes the **order** (hook before refresh), which is honored; it does not forbid the redundancy. Recorded so the next run does not rediscover it as a finding — and because `KCD-38`'s fan-in design pass is the natural place to remove it. |
| **A-9** | `debug-logging-§8` | Stale cross-references to a file deleted during the LibKa0s adoption: `core/Constants.lua:91` and `core/CoreSetup.lua:20` both name `modules/DebugLog.lua`, which no longer exists. Comment drift, not a rule failure; sweep it with `KCD-45`, which is in the adjacent file. |

---

**Counts — MUST: 17 · SHOULD: 2 · advisory: 9.**
Recurring: `KCD-30`, `KCD-31`, `KCD-32`, `KCD-33`, `KCD-34`, `KCD-35`, `KCD-37`, `KCD-38`.
New: `KCD-40`..`KCD-49`. Closed since 2026-08-04: `KCD-36`. Withdrawn by the standard: `KCD-39`.

**Anti-patterns triggered:** #17 (`KCD-38`), #35 (`KCD-33`, `KCD-46`), #46 (`KCD-47`),
#47 (`KCD-30`, `KCD-31`, `KCD-32`, `KCD-33`, `KCD-40`).
**Not triggered, checked:** #48 (the `LibKa0s/` folder is whole and the TOC lists the single
`LibKa0s.xml`), #49 (no `docs/agent-context.md`), #51 (the complexity report is byte-identical to
today's measurement), #53 (no watch-list entry is dispositioned "accepted"; all four point at tracked
IDs), #54 (no `or`-defaulting sweep over stored settings), #26/#27/#34/#40 (root doc set and the
three-place standards reference are complete).
