# KickCD — Deviations (2026-08-04)

**Addon:** Ka0s KickCD · **Standard:** v2.17.1 (2026-08-03) · **Prefix:** `KCD-`

**Provenance.** The standard was fetched over the network with `curl -fsSL` from
`https://raw.githubusercontent.com/tusharsaxena/WowAddonStandards/master` — `AUDIT.md`,
`standards/STANDARDS.md` and **all 24 section files** named in the index's Sections list — and every
fetched file was verified **byte-identical** to the canonical checkout at
`/mnt/d/Profile/Users/Tushar/Documents/GIT/WowAddonStandards` (clean tree, HEAD `2141229`). Nothing
below is measured against a remembered rule; the commands and their output are in `03_EVIDENCE.md`
§1.

**Context.** `docs/audits/2026-07-12/` (`KCD-01`..`KCD-23`) and `docs/audits/2026-07-18/`
(`KCD-24`..`KCD-29`) are both fully closed — spot-verified in `03_EVIDENCE.md` §9. `KCD-27` is
closed by the standard rather than by the addon: v2.17.0 deleted the fourth standards-reference
place along with `docs/agent-context.md`. New IDs run `KCD-30`..`KCD-39`; `KCD-19` recurs as a watch
item at a reduced severity, explained in its row.

---

## MUST

| ID | Section(s) | Sev | Deviation | Fix direction |
|---|---|---|---|---|
| **KCD-30** | `testing-§1`, `testing-§9`, AP-47 | MUST | The shared test kit is **vendored but not consumed**. `tests/_kit/` is byte-identical to LibKa0s's `testkit/`, yet `tests/run.lua:19-83` hand-rolls the case registry, the assertion set and the `--list` renderer; `tests/loader.lua` is a private sandboxed source loader; and `tests/wow_mock.lua` (1049 LOC) is a full replacement whose own comment at `:556-558` calls its widget builder "a VERBATIM port of `tests/_kit/mock_base.lua`'s builder". testing-§1 makes all four a MUST NOT ("MUST NOT hand-roll their own registry, assertion set, source loader, or base mock"), requires `run.lua` to `dofile` the kit's `framework.lua`/`loader.lua` and hand its suites to `Kit.run`, and requires `wow_mock.lua` to be a **thin extender** over `mock_base.lua`. testing-§9 additionally requires the runner's own-file list to be derived with the kit's `Loader.tocFiles`. | Adopt the kit: `Kit = dofile("tests/_kit/framework.lua")`, `Kit.expose(_G.KICKCD_TEST)`, `Kit.run{ dir = "tests/", suites = {...} }`; delete the private framework block and `tests/loader.lua`; rebuild `tests/wow_mock.lua` as `local base = dofile("tests/_kit/mock_base.lua")` plus the per-key overwrites this addon genuinely needs. The assertion names and semantics already match (`tests/run.lua:73-83`), so no suite file changes. Re-derive the TOC list through `Loader.tocFiles("KickCD.toc")`, keeping the explicit `LibKa0s.xml` file list and the case that pins it. |
| **KCD-31** | `options-ui-§1`, AP-47 | MUST | Host copies of **library-provided Options members** are installed onto the library instance, so the library's own widget makers run KickCD's code: `Helpers.LSMValues` (`settings/Panel.lua:281`), `Helpers.AttachTooltip` (`:341`), `Helpers.EnsureScroll` (`:394`), `Helpers.AddSpacer` (`:443`). The library supplies all four — `libs/LibKa0s/OptionsWidgets.lua:152,181`, `libs/LibKa0s/Options.lua:305,485` — and its makers reach them **through the instance** (`OptionsWidgets.lua:193,218,241,248,255,269,…`). options-ui-§1 permits decorating the instance only with "pieces that did not generalize"; these generalized and shipped. The `LSMValues` pair is also a live contract difference: the host's returns a **table**, the library's returns a **closure**. | Delete the four host implementations and their `Helpers.*` assignments; point `BuildMainContent` and the other in-file callers at `Helpers.EnsureScroll`/`Helpers.AddSpacer`. Sweep the `LSMValues` call sites — each is currently `values = function() return H.LSMValues(m) end` (`settings/Icons.lua:196,220`; `settings/Label.lua:147`; `settings/Castbar.lua:280,401,438,463,500`) and becomes `values = H.LSMValues(m)`, since the library hands back the `values` function itself. Note the user-visible empty-media fallback key moves from `"Default"` to the library's `LSM_NONE`. `Helpers.AnchorValues`/`AnchorOrder` are genuinely host-shaped and stay. |
| **KCD-32** | `options-ui-§8`, AP-47 | MUST | Host copies of the library's **layout constants**. `core/Constants.lua:66` declares `Const.PANEL_PADDING_X = 16`, restating `lib.LAYOUT.PADDING_X` (`libs/LibKa0s/Options.lua:46`); `settings/Panel.lua:426` declares `local ROW_VSPACER = 8`, restating `lib.LAYOUT.ROW_VSPACER` (`Options.lua:56`), and `:430` then **overwrites the library's published `O.ROW_VSPACER`** (`Options.lua:159`) with it. options-ui-§8: "Hosts **MUST NOT** copy these values into their own constants file… read it off the instance (`Helpers.ROW_VSPACER`, …) rather than restating the number." The file's own comment at `settings/Panel.lua:300-306` cites the rule and then, twenty lines later, breaks it — which is the drift the rule names. | Delete `local ROW_VSPACER = 8` and the `Helpers.ROW_VSPACER = ROW_VSPACER` assignment; read `Helpers.ROW_VSPACER` at the two call sites (`settings/Panel_Render.lua:20` already does). For `PADDING_X` the library does not publish it today, so the compliant route is an **additive** publication of `O.PADDING_X` in `LibKa0s-Options-1.0`, pushed upstream, re-vendored, and then read off the instance — never a local edit under `libs/` (library-stack-§7). Until the upstream minor lands, keep `Const.PANEL_PADDING_X` but record it in the file as a deliberate, time-boxed deviation naming this ID. |
| **KCD-33** | `events-frames-taint-§8`, AP-47, AP-35 | MUST | A **second secret-safe stringifier** lives in `core/Compat.lua:377-387` — a private `safeRender` inside `Compat.DebugInterrupt`. Two rules bite. (a) events-frames-taint-§8: the stringifier is `LibKa0s-Core-1.0`'s and an addon "**MUST NOT** hand-roll either"; the **only** sanctioned second copy is the library-absent branch of `core/CoreSetup.lua`, which already exists (`:81-92`). (b) Its detection is `_G.issecretvalue` (`:378`, and again at `:401`), not the mandated `table.concat` probe — so the guard in the command whose entire purpose is diagnosing secrets is the one guard not built the mandated way. Adjacent: `core/Compat.lua:372` falls back to the global `print()`, which §8 forbids at a call site. | Delete `safeRender` and route every value through `NS.SafeToString` (published from `core/CoreSetup.lua:110`, the library's own function). Keep the `%q` quoting for plain strings by wrapping the shared result, not by re-deriving safety. Replace the `_G.print` fallback at `:372` with the shared printer alone. Keep the `issecretvalue` *reporting* column at `:401` — reporting whether a value is secret is the command's diagnostic payload; it must simply stop being the safety mechanism. |
| **KCD-34** | `performance-§9`, `performance-§2`, `testing-§7` | MUST | **`tests/perf.lua` does not exist.** performance-§9 makes the offline scenario runner a MUST — at that exact path, run as `lua tests/perf.lua`, outside the green gate, asserting only deterministic quantities (API calls, bytes allocated) — and requires a **zero-overhead scenario** pinning that the hottest bracketed path with capture off allocates no more than the same path with instrumentation absent. performance-§2 names that scenario the **required evidence** for the gated-bracket rule: "the claim 'instrumentation is free when off' is not a comment; it is a measured, committed number." Today it is a comment. | Add `tests/perf.lua`: derive its addon-file list through the kit's `Loader.tocFiles` (testing-§9), load the addon under the mock, and ship at minimum the zero-overhead scenario over `iconApply` — `core/PerfSetup.lua:29-31` already names it as the addon's only real hot path — measuring `collectgarbage("count")` deltas with a full collect either side, capture off vs. `NS.Perf` stubbed absent. Assert allocation and call counts only; never wall clock. Keep it out of `tests/run.lua`, and pin the derivation call by reading the source from a gated suite. |
| **KCD-35** | `documentation-§3`, `performance-§8` | MUST | Two **required topic-detail docs are absent**: `docs/performance.md` and `docs/perf-runs/README.md`. documentation-§3 names three required topic-detail docs; only `docs/test-cases.md` exists. performance-§8 additionally requires `docs/perf-runs/` to be a standing, cumulative capture store with a README documenting the naming convention, a schema summary, and a pointer to the library's canonical record contract. (Deliberately withheld by the 2026-08-03 review as this audit's job — `docs/reviews/2026-08-03/05_FINAL_SUMMARY.md:204-207`.) | Write `docs/performance.md`: which paths are bracketed and why (the reasoning is already written, in `core/PerfSetup.lua:19-31` and `:104-122` — move it, do not duplicate it), how to run `/kcd perf`, how to read the report, and what the harness can and cannot resolve; point at the library for the shared protocol rather than restating it. Create `docs/perf-runs/README.md` with the `<YYYY-MM-DD>-<source>-<label>.json` convention, a schema summary and the pointer upstream. Commit the first capture worth keeping alongside it. |
| **KCD-36** | `documentation-§1` #2, `testing-§5` | MUST | The README `[tests]` badge is **stale**: `README.md:7` reads `Tests-646%2F646_passing` while the authoritative generated inventory `docs/test-cases.md:829` totals **648**, and `lua tests/run.lua` reports **648 passed, 0 failed**. Both sections make the lockstep a MUST DO in the *same change* that moves the count, never a follow-up; the badge is static, so it goes stale silently on exactly the surface players read. | Update `README.md:7` to `Tests-648%2F648_passing`. Then make it mechanical: the `/wow-addon:bump-version` and suite-changing flows both already regenerate `docs/test-cases.md` — regenerate and edit the badge in the same commit. `CLAUDE.md:56` already states the rule; this is the rule being missed in practice, not being absent. |
| **KCD-37** | `documentation-§1` | MUST | An **angle-bracket placeholder in shipped README content**: `README.md:191` renders ``/kcd reset <setting>`` inside the Troubleshooting table. documentation-§1 is explicit that CurseForge strips `<setting>` as unknown HTML **including inside backticks**, so the line reaches players as `/kcd reset` with the placeholder silently deleted, while looking correct on GitHub. The rest of the README already uses the bare form (`README.md:73,74,75`), so this is one straggler, not a policy. | Change to ``/kcd reset setting`` to match `README.md:75`. The `<br>` tags in the Version History cells (`README.md:210,211`) are deliberate HTML and **MUST NOT** be swept — documentation-§1 protects them by name. |
| **KCD-38** | `architecture-§4`, AP-17 | MUST | **Six senders for one bus message.** `Ka0s_KickCD_CONFIG_CHANGED` is sent from `core/KickCD.lua:128`, `core/KickCD.lua:457`, `modules/IconGrid.lua:522`, `modules/Castbar.lua:333`, `settings/Spells.lua:325` and `settings/Panel.lua:77`. architecture-§4: "**MUST NOT** have two senders for the same message" (AP-17), and "**MUST** document each message… with: name, sender (**one**), payload schema, all consumers." Recorded honestly in-repo as an intentional multi-module message (`docs/message-bus.md:61`), which makes it a *known* deviation rather than an unknown one — but the standard's escalation path is that the user classifies it as an accepted deviation or changes the standard, and until one of those happens the audit records it. | This is a design pass, not an edit — the 2026-08-03 review reached the same conclusion (`05_FINAL_SUMMARY.md:196-200`). Two viable ends: (a) narrow to a single owning module — the natural owner is `settings/Panel.lua`'s `FireConfigChanged`, with the other five call sites routing through it; or (b) take it upstream as a standard change carving out a "settings-changed" fan-in message shape. Do not leave it in the middle: `docs/message-bus.md`'s own rule ("a second emitter… must live in the same file as the first") is already violated by the current five files. |

## SHOULD

| ID | Section(s) | Sev | Deviation | Fix direction |
|---|---|---|---|---|
| **KCD-39** | `performance-§10` | SHOULD | `docs/complexity.md` is absent. performance-§10 SHOULDs a committed, **report-only** `lizard` run over the addon's own source excluding `libs/`. The section itself notes absent tooling makes the report stale rather than the addon non-compliant, so this is a genuine SHOULD with a low ceiling — but the addon has three files in the 1000–1500 LOC band (`KCD-19`) and that is exactly the decision the report is meant to inform. | `lizard core modules settings defaults locales -l lua > docs/complexity.md`, with a generated-by line at the top naming the command. **MUST NOT** gate commits on it. Regenerate when a file it names changes materially. |

## Advisory (MAY / recorded, not deviations)

These are recorded so a later run does not rediscover them as findings, and so the reasoning survives.

| ID | Section(s) | Note |
|---|---|---|
| **A-1** (was `KCD-29`) | `library-stack-§1` vs `library-stack-§3` | `libs/AceTimer-3.0/` was deleted since the last audit; the addon schedules through `C_Timer.After` and never calls `LibStub("AceTimer-3.0")`. That satisfies §3's "**MUST** vendor only libs the addon actually `LibStub`" and departs from §1's mandatory-lib table. The two rules cannot both be satisfied here — this is a **standard-internal tension**, not an addon defect, and the addon has taken the branch that keeps `libs/` honest. Raise it upstream if it recurs across the collection; do not re-vendor AceTimer just to satisfy a table. |
| **A-2** | `layout-§1` (was `KCD-19`) | `modules/Castbar.lua` (1296), `modules/IconGrid.lua` (1100) and `settings/Spells.lua` (1047) sit in the 1000–1500 "on notice" band. **Reclassified from SHOULD to advisory**: layout-§1 carries a MUST at 1500 and the words "on notice" for the band — there is no SHOULD verb attached to it, and inflating a watch statement into a graded failure is the kind of drift an audit should not add. Castbar has *fallen* from 1473 at the last audit to 1296 after two peels (`modules/Castbar.lua:3-11`), so the trend is the right way. Keep watching; no action. |
| **A-3** | `packaging` | `.superpowers/` (54 files) and `.claude/settings.local.json` are dev-only and are **not** in `.pkgmeta`'s ignore list. The section's MUST names `docs/`, `_dev/`, `tests/` and lockfiles, all of which are covered, so this is not a deviation — but two ignore lines would make the package unambiguously clean. |
| **A-4** | `documentation-§1` | The README carries an extra `## Libraries and credits` section between Troubleshooting and Issues (`README.md:193-200`). The canonical sections' **relative order is preserved**, which is what §1 requires, and the section is player-useful (it answers "which LibKa0s does this build carry?"). Not a deviation. |
| **A-5** | `documentation-§2` | `CLAUDE.md` carries an extra `## The docs/ set — there is no agent-context.md` section (`:19-34`) between the Standards-compliance section and the pointer list. The five required items are all present in order around it, and the inserted section is precisely the v2.17.0 prohibition being enforced in project memory. Not a deviation. |
| **A-6** | `documentation-§4` | `docs/pending/LEDGER.md` is a **decision** ledger (done / wont-do / deferred, produced by `/wow-addon:pending-audit`), not a `TODO.md` backlog, and §4's prohibition names `TODO.md` specifically. Not a deviation — but its `deferred` rows are, functionally, a second place work can hide from GitHub issues. Worth keeping short. |
| **A-7** | `toc-file-§1` | `KickCD.toc:14` keeps a commented-out `X-Wago-ID` placeholder with a `TODO(KCD-18)` marker. `X-Wago-ID` is a MAY and a commented line is an omitted field, so the block's field order and no-blank-lines rules both hold. Not a deviation; delete or fill it whenever Wago publication is decided. |

---

**Counts — MUST: 9 · SHOULD: 1 · advisory: 7.**

**Anti-patterns triggered:** #17 (`KCD-38`), #35 (`KCD-33`, detection half), #47 (`KCD-30`,
`KCD-31`, `KCD-32`, `KCD-33`).
**Not triggered:** every other numbered anti-pattern #1–#49, verified in `01_CURRENT_STATE.md` and
`03_EVIDENCE.md` — including #45 and #48, both cleared by empty `diff -r` runs against the LibKa0s
source repo, and #49, cleared by deletion and a standing in-repo prohibition.
