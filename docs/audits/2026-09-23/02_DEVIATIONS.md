# KickCD — Deviations (2026-09-23)

**Addon:** Ka0s KickCD 1.3.0 · **HEAD:** `dc11094` · **Standard:** **v2.64.0 (2026-09-23)**
**Prefix:** `KICKCD-`. Rows carried over from earlier runs keep their `KICKCD-A-` and `KICKCD-B-` ids.
Rows that are new in this run use the `KICKCD-C-` series.

## The two tallies, and their basis

| Tally | Count | Basis |
|---|---|---|
| **Headline (roots only)** | **23** | One row per independent cause. The four `derived from` rows are left out. |
| **Total including dependents** | **27** | 23 roots, plus `KICKCD-C-16a` to `C-16d`, which are derived from `KICKCD-C-16` |
| **MUST failures, roots only** | **16** | `C-01, C-03, A-08, B-02, B-03, C-02, C-04, C-05, C-06, C-07, C-08, C-10, C-11, C-13, C-15, C-16` |
| **MUST failures including dependents** | **20** | The 16 above plus `C-16a` to `C-16d`, which are toc-file-§5 MUSTs, one row per position |
| **SHOULD failures** | **2** | `A-02`, `C-09` |

**By impact grade, roots only:** High **1** · Medium **1** · Low **16** · Info **5**.
**By impact grade, including dependents:** High **1** · Medium **1** · Low **20** · Info **5**.

**What the grades mean here.**

- The one **High** is graded High because `AUDIT.md`'s re-vendor check says so in as many words: *"A
  tag vendored with no bundle naming it and no `## Documented deviations` row saying why is a
  **High** finding."* It is a records gap, and no player can reach it. That conflicts with the
  playbook's own step-5 grading table, and `C-01` says so rather than quietly regrading.
- The one **Medium** is `C-03`. Every per-unit toggle, every `/kcd disable` + `enable` and every perf
  capture's suspend and resume leaks 36 unit-filter frames. A player can reach it, the result is
  degraded behavior rather than a break, and nothing is visible.
- Everything else is a document, a comment, a config file, an issue label or a latent code shape.
  None of it can reach a user, `KickCDDB` or a game session today. Every row still names the MUST or
  SHOULD it fails.

## Movement since 2026-09-08 (v2.39.0)

The previous run filed **8** roots. **One is closed** (`B-01`). **Seven recur** and keep their ids
(`A-02`, `A-08`, `B-02`, `B-03`, `B-04`, `B-05`, `B-06`). Part (b) of `B-06` now lives in `C-10`.
**Sixteen roots are new** (`C-01` to `C-16`), and most of them exist because v2.40.0 to v2.64.0
made them checkable. 7 recurring + 16 new = 23 roots.

| Prior id | Status today |
|---|---|
| `KICKCD-A-08` | **Recurs.** The same three `or _G.print` arms have moved lines: `core/KickCD.lua:127`, `core/Compat.lua:441`, `modules/Cooldowns.lua:661`. |
| `KICKCD-B-01` | **Closed.** `perf-analysis/README.md` now sits in `### Conditional` (`docs/ARCHITECTURE.md:355`), and `### Verification and record` holds exactly six rows (`:362-367`). |
| `KICKCD-B-02` | **Recurs**, for a new reason. The hand-written `tests/test_spelling.lua` has been replaced by the kit's `test_prose`. That gate skips the whole `docs/perf-analysis/` directory (`tests/_kit/test_prose.lua:211`), so the two British spellings at `docs/perf-analysis/README.md:35-36` are still unseen. |
| `KICKCD-B-03` | **Recurs, unchanged.** `docs/ARCHITECTURE.md:390` still says "Two profile migrators". This run also finds that both register rows' `schema.md#L256` / `#L266` anchors have moved away from the text they cite. |
| `KICKCD-A-02` | **Recurs.** Three conventional notes were added (Env, Pool, Launcher). The groups are still unmarked. |
| `KICKCD-B-04` | **Recurs**, re-measured: 38 commits of drift, and two band changes. |
| `KICKCD-B-05` | **Recurs.** #9 still carries `[Optional]`. |
| `KICKCD-B-06` | **(a) recurs.** (b) has moved into `C-10`, because what it describes is now the anti-pattern #53 decision-record finding rather than a stale label. |

## Recorded deviations — accepted, not re-filed, not counted

`docs/ARCHITECTURE.md:388-396` has **seven** rows. This run read the register before filing
anything. For every row it (1) resolved the cited rule against v2.64.0, (2) evaluated the re-check
trigger against HEAD, and (3) resolved every evidence id the row cites. Those are
`audit-review-history`'s three MUSTs.

| Rule | Decided | Rule changed since v2.39.0? | Trigger fired? | Evidence ids | Status |
|---|---|---|---|---|---|
| `savedvariables-§1`: shape-driven migrators | 2026-07-16 | Only by an additive load-pass bullet, which does not touch this row | Not decidable as the row is written. See `B-03` | `schema.md#L256` has moved away from its text | **Accepted, with `B-03` against the row's accuracy** |
| `savedvariables-§1`: `units.<unit>` restructure | 2026-07-15 | No | No. `defaults/Profile.lua` still declares only target and focus | `schema.md#L266` has moved away from its text (`B-03`) | **Accepted** |
| `options-ui-§1`: `LSMValues` / `AnchorValues` / `AnchorOrder` host-side | 2026-08-05 | Yes: runtime-completing, Compat and Bus stub shapes were added. None of them covers a host value provider | No. `settings/Icons.lua:56` and `settings/Castbar.lua:192` still call `H.AnchorValues()` at file load | Resolves | **Accepted** |
| `options-ui-§15`: the visibility value list | 2026-09-02 | Yes: Test mode, Minimap button, and the v2.49.0 exemption. The value set is unchanged | No. The composer still hard-codes `values = VISIBILITY_VALUES` (`libs/LibKa0s/OptionsCompose.lua:479`) | Resolves | **Accepted** |
| `events-frames-taint-§8`: `safeRender` | 2026-08-05 | Yes: registration rules were added to §1. §8's stringifier line is unchanged | No. The only callers are `describe` (`core/Compat.lua:384`) and `DebugInterrupt` (`:452`), both inside the dump | `:368-376` and `:380-385` resolve | **Accepted** |
| `options-ui-§14`: the two-row Spells band | 2026-09-22 | No change since the row was written | No. There is no compact `IdInput` in v1.55.0 | Resolves | **Accepted** |
| `architecture-§5`: spell-entry `enabled` / `category` | 2026-09-12 | No change since the row was written | No | #16 and #17 are both closed, `state:done` | **Accepted** |

**Declines that owe no row.** #11, #12 and #14 are closed `state:will-not-do` module declines. Under
library-stack-§3/§7's prune rule they are compliance. #12's text is stale (`C-12`).

---

## High

| ID | Section | Grade | Deviation | Fix direction |
|---|---|---|---|---|
| **KICKCD-C-01** | `audit-review-history` (*A re-vendor commit implies a bundle*), MUST | **High**, per the playbook. See the note | **25 LibKa0s tags have been vendored since the store's horizon, and no bundle records them.** The payload-derived check (`AUDIT.md`, *every re-vendor commit has its bundle*) reads the provenance tag from `CLAUDE.md` at every commit that touches `libs/LibKa0s/` since 2026-08-25. It finds 31 distinct tags. The eight bundles record 8 tags. The 25 tags vendored with no bundle and no register row are: v1.18.0, v1.18.1, v1.19.0, v1.23.0, v1.24.0, v1.26.0 to v1.29.0, v1.35.0, v1.36.0 to v1.36.2, v1.37.0, v1.38.0, v1.39.0, v1.42.0, v1.44.0, v1.45.0, v1.46.1, v1.47.0, and v1.50.0 to v1.53.0. There is one gap each side of the v1.30.0 to v1.34.0 run, and one from v1.35.0 to v1.53.0. **Grade note:** the playbook grades this High in so many words. Its own step-5 table says High is reserved for what a user can reach, and this is a history gap no player sees. The grade is reported as the playbook specifies, and the tension is recorded here for the standard's owner. | Write **one consolidated bundle** naming the span, as `audit-review-history` allows: *"A consolidated bundle is a compliant answer to a backlog."* That is `docs/revendor/<date>-v1.18.0-to-v1.53.0/` with `01_DELTA.md` and `05_SUMMARY.md`, listing the 25 tags and the commits that carried them (the sweeps). Do not back-fill a folder per tag. |

## Medium

| ID | Section | Grade | Deviation | Fix direction |
|---|---|---|---|---|
| **KICKCD-C-03** | `events-frames-taint-§1` (the unit-filter carve-out: *"MUST be re-used across a disable/enable cycle rather than rebuilt"*), MUST | Medium | **Every enable builds new unit-filter frames and throws the old ones away.** `Util.RegisterUnitCastEvent` (`core/Util.lua:438-445`) calls `CreateFrame("Frame")` every time it is called. `IconGrid:EnableUnit` makes 8 of them per unit (`modules/IconGrid.lua:810-821`) and `Castbar:EnableUnit` makes 10 per unit (`modules/Castbar.lua:1016-1031`). `DisableUnit` and `Suspend` unregister them and then drop them with `inst.eventFrames = {}` (`modules/IconGrid.lua:831-832`, `:860-861`; `modules/Castbar.lua:1044-1045`, `:1109-1110`). WoW never collects a frame, so each cycle leaks **36 frames** with both units on. A player reaches it with `/kcd disable` + `enable`, the Focus enable toggle, a profile switch that flips `enabled`, and each perf capture's suspend and resume. slash-commands-§7 says the same thing from its side: *"What goes is the registration, not the frame object … destroying it … would leak one frame per flick of the switch."* A second part of the same rule is also at issue: the helper accepts any `eventName`, which is the shape of the *"general-purpose private-frame factory"* the carve-out excludes. | Build the per-unit frames **once**, held on the instance, and on resume re-register the events on the same frames rather than creating new ones. One frame per (module, unit) can carry every `RegisterUnitEvent` call for that unit, since the carve-out allows plural calls and one `OnEvent`. That cuts 36 frames to 4. Narrow the helper to the cast-event family, or document its one job at the definition. Add a `tests/test_disabled.lua` case: count `CreateFrame` across disable, enable, disable, enable, and require the second count to equal the first. |

## Low — MUST failures

| ID | Section | Grade | Deviation | Fix direction |
|---|---|---|---|---|
| **KICKCD-A-08** | `events-frames-taint-§8`, MUST | Low | **Three `_G.print` fallback arms remain** at `core/KickCD.lua:127`, `core/Compat.lua:441` and `modules/Cooldowns.lua:661`. None of them can be reached, because `core/CoreSetup.lua` defines `Util.print` on both paths. The rule still forbids the global `print` "at any site". | Change each to `NS.Util.print` (or `self.Util.print`). Add a `tests/test_source_style.lua` case that asserts no authored file matches `or _G%.print`. |
| **KICKCD-B-02** | `localization-§5`, AP #46, MUST | Low | **Two British spellings are in a live, rewritten doc that the prose gate skips.** They are `docs/perf-analysis/README.md:35` "analysed" and `:36` "neighbours". The cause has moved upstream: the kit's `test_prose.lua:211` skips the **whole** `docs/perf-analysis/` directory, but localization-§5's exclusion is *frozen dated bundles*, and documentation-§3 says this README "is the one file in the store that is rewritten". `docs/automated-tests/README.md` and `RESULTS.md` are excluded the same way. | (1) Fix the two words here (US spellings: "analyzed", "neighbors"). (2) **Upstream, in LibKa0s `testkit`:** narrow `SKIPPED_DIRS` to the dated bundle subfolders so that the stores' `README.md` and `RESULTS.md` are scanned, then re-vendor. |
| **KICKCD-B-03** | `documentation-§3` (the register's accuracy), `audit-review-history` (third MUST), MUST | Low | **The first register row describes a state the tree has left.** `docs/ARCHITECTURE.md:390` says "**Two** profile migrators run off the stored shape". The tree runs **three** of them ungated: `FoldLegacyUnits`, `BackfillLabelStyle` and `MigrateSpecKeys` (`core/Database.lua:821-824`), and the hub says so itself at `:468`. The trigger's second arm, "a third shape addition under an existing profile field", is left for the owner to evaluate. Also, both `savedvariables-§1` rows cite `schema.md#L256` / `#L266`. Line 256 is now a comment inside the `label.style` code block, and the migration reasoning is at `docs/schema.md:269`, under the `### units.<unit>.label.style shape` heading (`:241`). | Decide the trigger. Either retire the row, since `BackfillLabelStyle` may be the "third shape addition", or rewrite **What differs** to name all three migrators and restate the trigger. Replace the line anchors with heading anchors (`schema.md#unitsunitlabelstyle-shape`, and the `## Migration: …` headings at `:273` onward) so they cannot rot. |
| **KICKCD-C-02** | `events-frames-taint-§1` (*An unknown event name raises*), MUST | Low | **No registration block survives one bad name, and no rejected-name record exists.** The blocks register bare, one after another: `modules/Cooldowns.lua:544-555`, `modules/IconGrid.lua:935-949`, `modules/Castbar.lua:1081-1087`, `modules/UnitLabel.lua:253`, `core/State.lua:144-146` and `settings/Spells.lua:380-381`, `:1358-1364`. A grep for `pcall` around registration, `IsEventValid`, or a rejected-name list finds nothing. It is latent: every name is valid on 12.1.0 today, and the suite passes. The first retired name would silently stop the rest of its block, for example the three `Cooldowns` events after it. | Add one `pcall`ed `NS.Util.SafeRegister(target, event, handler)` helper that records rejected names in a table. Add a `/kcd debug events` verb, or a console line, that lists them. Route every block above through it, fronting it with `C_EventUtils.IsEventValid` (the SHOULD). Test it with the kit mock's `M.__badEvents`. |
| **KICKCD-C-04** | `events-frames-taint-§1` (*MUST NOT create per-module frames to carry ordinary event traffic*), MUST | Low | **Two private frames carry non-unit events.** `core/State.lua:143-146` carries `PLAYER_LOGIN` and `PLAYER_REGEN_DISABLED` / `_ENABLED`. `settings/Spells.lua:375-381` carries `TRAIT_CONFIG_UPDATED` and `PLAYER_SPECIALIZATION_CHANGED`. Neither is a `RegisterUnitEvent` filter, so the carve-out does not cover them. Both stand down correctly. The State frame is argued for at `docs/ARCHITECTURE.md:99` (combat-flag ordering), but it has no register row. | Move both onto AceEvent targets. For `State`, that is an `NS.NewBusTarget()` target or an AceEvent-embedded `State` table, and `NS.State.StandDown` / `StandUp` do not change shape. **Or**, if the combat-flag ordering really needs a raw frame, write a `## Documented deviations` row with a re-check trigger. |
| **KICKCD-C-05** | `documentation-§5` (docs in sync), `documentation-§1`, MUST | Low | **The README tells players the settings panel opens itself after combat, and it does not.** `README.md:97` says "`/kcd config` waits until combat ends". `README.md:113` says "it opens the moment combat ends". `NS:OpenSettings` refuses and returns (`core/KickCD.lua:815-819`), which is what options-ui-§2 requires: *"MUST NOT defer-and-replay the open on `PLAYER_REGEN_ENABLED`"*. This is player-facing text, but it is a doc, so the grade is Low. | Rewrite both cells: the game blocks the panel in combat, and you run `/kcd config` again afterwards. Run the de-AI pass (documentation-§1). |
| **KICKCD-C-06** | `documentation-§3` (Tier 2 `debug.md`), MUST | Low | **The `debug.md` "Not applicable" row gives a false reason.** `docs/ARCHITECTURE.md:356` says the `/kcd debug` sub-commands "dump state through [the console] rather than adding a surface". The three dumps print to **chat** through `NS.Util.print`: `core/Compat.lua:441`, `modules/Castbar_Debug.lua:134` and `modules/Cooldowns.lua:661`, reached from `core/KickCD.lua:307-325`. That is arguably a debug surface beyond the default console, so the trigger may have fired. The playbook grades a false "Not applicable" row above a bare omission, and this is still Low because it is a doc. | Either write `docs/debug.md`, covering the three dump verbs, what each prints and when to use it, which also absorbs part of `castbar.md`'s "debug dump" and `midnight-quirks.md`'s interrupt-dump notes. **Or** route the dumps into the console with `NS.Debug` and keep the row, with its reason corrected. |
| **KICKCD-C-07** | `documentation-§3` (*"Shims `LibKa0s` supplies … MUST NOT be re-documented here"*), MUST NOT | Low | **`docs/compat-layer.md` restates the contracts of six LibKa0s-Compat members.** The rows at `:13-14`, `:16-17` and `:23-24` (`IsSecret`, `GetSpellCooldown`, `GetSpellInfo`, `GetSpellTexture`, `GetSpecialization`, `GetSpecializationInfo`) spell out return shapes and ladders that the library documents in `docs/api/Compat/version-1-docs.md`. This came in with today's v1.55.0 adoption. | Collapse the six rows into one line: "wired from `LibKa0s-Compat-1.0` by name; contract in LibKa0s `docs/api/Compat/version-1-docs.md`". Keep only the addon-specific caveats, such as "use `isActive`". |
| **KICKCD-C-08** | `documentation-§5`, the documentation lane's *inventory matches the tree*, MUST | Low | **Six stated inventories or citations no longer match the tree.** One rolled-up finding, because the fix is one sync pass. (1) `docs/ARCHITECTURE.md:77-81` says five files read `addonName` and thirty write `_`. The tree has **8 and 30** (Constants, LauncherSetup and LifecycleSetup are left out), and `.luacheckrc:24-28` repeats "five". (2) `docs/ARCHITECTURE.md:352`, the compat-layer trigger, says "496 lines". The file is **485**, and §3's trigger is a **shim count**, which is **8** by §3's own grep. (3) `:353`, the message-bus row, states no trigger. There are 5 messages, below "more than ten". (4) `:281` and `core/LifecycleSetup.lua:113` say "five subscriptions" for the Spells page. `Spells.StandDown` releases **7**: 5 on the bus target and 2 on `cacheEvents`. (5) `.luacheckrc:89` cites `tests/run.lua:217`, and the global is written at **`:299`**. (6) `.pkgmeta:17`, `:20` and `:21` cite `packaging.md:28`, which is now a template comment line. The rule is `packaging` (it has no numbered subsections). | Correct every figure from the tree with a recorded command. Replace line citations into other files with a symbol or a section id (`packaging`, `KICKCD_TEST = Kit.expose`), because line numbers rot. |
| **KICKCD-C-10** | `automated-tests-§4`, AP #53, MUST NOT | Low | **The watch list is a backlog in a watch list's format.** All four band entries (`docs/automated-tests/RESULTS.md:84-87`) have read "Already tracked as `A-2`" or "`KCD-30`" with "Watch, no action" across **four consecutive recorded runs** (`20260908-181321`, `20260910-234511` for release 1.3.0, and both `20260916` runs). The trackers they cite are the retired advisory series (`A-2` is `docs/audits/2026-08-05/02_DEVIATIONS.md:50`), so a reader follows an id to nothing. Spells has since passed its own "re-check at 1400" line (it is now 1444). | At the next release run, give each entry either a live tracker (an issue naming the seam) or a fix. Retire `A-2` and `KCD-30` from the cells. Write `modules/IconGrid_Render.lua`'s first disposition when it enters the band. |
| **KICKCD-C-11** | `audit-review-history` (the issue store: `state:done` is terminal and closed), MUST | Low | **Issue #15 is OPEN and labeled `state:done`.** The two disagree, so a reader cannot tell whether the GCD attribution work is finished. The 1.3.0 Version History row cites #15 as fixed (`README.md:125`). | Close #15 if the fix shipped in 1.3.0. Otherwise relabel it `state:triaged`. |
| **KICKCD-C-13** | `library-stack-§8` (*"The catalog is the vocabulary (MUST)"*), AP #63, MUST | Low | **A one-off Blizzard atlas where the catalog has a mark.** The Spells page's remove button draws `transmog-icon-remove` (`settings/Spells.lua:798`). The vendored catalog carries `close`, `clear` and `cancel` (`libs/LibKa0s/Media.lua:94`, `:97`), which `NS.Icon` resolves. It is cosmetic, and the look drifts from the other Ka0s list editors. | Draw `NS.Icon("clear")`, or `close`, through the existing `makeRowIconBtn`. If none of the marks fits "remove from list", add one upstream. |
| **KICKCD-C-15** | Index *Reading this document* (`filename-§N` is *"the only cross-reference form — the old global §N.M numbering is retired"*), `documentation-§5`, MUST | Low | **Eight bare `§N` citations that name no file.** They are `core/Compat.lua:245` "(§11)", `modules/IconGrid.lua:1207` "(§9)", `settings/Panel.lua:179` "standard §10", `tests/test_compat_api.lua:4` "(§11)", `tests/test_debuglog.lua:108`, `:115` and `:124` "(§11)" (repeated in `docs/test-cases.md:403-405`), and `tests/test_settings_log.lua:1` "(§10)". Each is the retired global numbering. For example, `compat` has no §11. Continuation shorthand after a qualified reference in the same sentence (`launcher-§1`/`§3`) is not counted. The scope of the count is in `03_EVIDENCE.md` §E9. | Qualify each: `compat`, `debug-logging-§9`, `debug-logging-§10`, `debug-logging-§11`. Regenerate `docs/test-cases.md` after renaming the three test titles. |
| **KICKCD-C-16** | `toc-file-§5` (*"A line whose position is load-bearing MUST carry a comment … naming what resolves at load"*), MUST | Low | **`KickCD.toc:96`, `modules\IconGrid_Layout.lua`, is load-bearing and has no annotation.** The file calls `NS:GetModule("IconGrid")` at file scope (`modules/IconGrid_Layout.lua:15`), and AceAddon's non-silent `GetModule` raises if `IconGrid.lua` has not loaded. The failure is loud rather than silent, but by §5's definition the position is load-bearing. This is the root of five positions that share one cause: the peeled siblings reopen their parent module at file scope. | Put one comment above each peeled group: `# LOAD-BEARING POSITION: the IconGrid_* / Castbar_* siblings call NS:GetModule(<parent>) at file scope, so each follows its parent`. That is two comments, which closes the root and all four dependents. |
| ↳ **KICKCD-C-16a** | `toc-file-§5`, MUST | Low | *derived from C-16.* `KickCD.toc:97`, `modules\IconGrid_Render.lua`: `NS:GetModule("IconGrid")` at `modules/IconGrid_Render.lua:19`. | Same comment. |
| ↳ **KICKCD-C-16b** | `toc-file-§5`, MUST | Low | *derived from C-16.* `KickCD.toc:99`, `modules\Castbar_Handle.lua`: `NS:GetModule("Castbar")` at `modules/Castbar_Handle.lua:24`. | Same comment. |
| ↳ **KICKCD-C-16c** | `toc-file-§5`, MUST | Low | *derived from C-16.* `KickCD.toc:100`, `modules\Castbar_Skin.lua`: `NS:GetModule("Castbar")` at `modules/Castbar_Skin.lua:53`. | Same comment. |
| ↳ **KICKCD-C-16d** | `toc-file-§5`, MUST | Low | *derived from C-16.* `KickCD.toc:101`, `modules\Castbar_Debug.lua`: `NS:GetModule("Castbar")` at `modules/Castbar_Debug.lua:10`. The file's own comment says "registered by modules/Castbar.lua, which loads first", but the TOC line does not. | Same comment. |

## Low — SHOULD failures

| ID | Section | Grade | Deviation | Fix direction |
|---|---|---|---|---|
| **KICKCD-A-02** | `toc-file-§5` (*conventional positions SHOULD say so, once per group*), SHOULD | Low | Three conventional notes now exist: EnvSetup `:44-46`, PoolSetup `:47-49` and LauncherSetup `:69-73`. The groups `# Locales` (`:39`), `# Defaults` (`:89`) and `# Modules` (`:93`) carry no conventional note, the rest of the `# Settings` block after Panel (`:120-127`) carries none either, and neither do the unannotated `# Core` lines (`:43`, `:54-56`, `:65-68`). §5's grading gives this one SHOULD row for the file. | Add one comment per group. Fold it into the same edit as `C-16`. |
| **KICKCD-C-09** | `documentation-§3` (*"The whole file SHOULD stay under roughly 400 lines"*), SHOULD | Low | **`docs/ARCHITECTURE.md` is 468 lines.** No mandated section is past roughly 60 lines: Settings Schema is `:169-211`, and the register with its census is `:376-440`. The weight is in non-mandated material. `## The stand-down: disabled is total` runs 85 lines (`:226-310`), and the External-dependencies LibKa0s bullet is a single 2,900-character line (`:117`). | Move the stand-down narrative into `slash-dispatch.md`'s disabled-state section, or into a Tier 3 `stand-down.md`, and leave a summary and one link. Split `:117` into a short list that links to `module-map.md`. |

## Info

| ID | Section | Grade | Note |
|---|---|---|---|
| **KICKCD-B-04** | `automated-tests-§4`, AP #51 | Info | **The record trails HEAD by 38 commits.** The newest row, `20260916-184417`, measured `fac2411`. HEAD is `dc11094`, with `git rev-list --count fac2411..HEAD` = 38. The row has no commit cell because it predates kit revision 25, so it is a pre-rule row and its commit is carried as *unknown*. Since that run: tests 973 → **1050**, lint files 97 → **101**, NLOC 19884 → **20793**, functions 2494 → **2640**. Max CCN is still **15** and there are still **0** warnings. `modules/IconGrid_Render.lua` (1014) entered the band. The last release tag is `1.3.0-release` (`ed88759`, 2026-09-10), and the checkpoint is **release**, not commit, so this is recorded rather than filed against the addon. |
| **KICKCD-B-05** | `audit-review-history`, AP #62 (by analogy) | Info | Issue #9 still carries `[Optional]`. That is not a `state:` value, so this is not #62. Strip it when the issue is next edited. |
| **KICKCD-B-06** | `documentation-§3` | Info | `docs/ARCHITECTURE.md:367` still describes `RESULTS.md` as "generated, never hand-edited". documentation-§3's template row reads *"generated by the runner, never hand-edited apart from the watch list's `Disposition` column"*. This is a one-line edit. Part (b) of the prior row now lives in `C-10`. |
| **KICKCD-C-12** | `audit-review-history` | Info | **Closed decline #12 is contradicted by the tree.** Its title reads "LibKa0s-Widgets-1.0: declined — no control in this addon wants it", but the addon now consumes `LibKa0s-Widgets-1.0` twice: `ReorderList` in `settings/Spells.lua` and `DragHandle` in `modules/Castbar_Handle.lua:41`. It owes no register row. Add a comment to the issue recording the adoption, so a later re-vendor does not read the decline as current. |
| **KICKCD-C-14** | `slash-commands-§2` (reserved `enable` / `disable`), open to interpretation | Info | **`/kcd spells enable\|disable <id>` reuses the reserved verbs for a spell entry** (`core/KickCD.lua:733-736`). §2 says *"MUST NOT re-use either verb for anything else — enabling a module, a feature or a unit is that thing's own verb or row"*. The sub-tree keeps dispatch unambiguous, and the standard's own `debug` example in §3 uses "enable/disable" for sub-arguments. **Not filed as a MUST failure** until the standard says whether the reservation reaches into a sub-tree. Raise that question upstream. If the ruling is yes, rename the sub-verbs to `track` / `untrack`. |
