
KickCD test harness
===================

test_util.lua
  PASS  Util.Unpack array-style color
  PASS  Util.Unpack hash-style color
  PASS  Util.Unpack nil defaults to opaque white
  PASS  Util.NormalizeSpecToken strips whitespace and upper-cases
  PASS  Util.PlayerSpecID returns the numeric spec ID, not the localized name
  PASS  Util.SpecTokenForID maps a spec ID to its English token
  PASS  Util.SpecDisplay prefers the English token and falls back to the raw ID
  PASS  Util.ResolveSpecID accepts a number, a numeric string and an English token
  PASS  Util.ResolveSpecID rejects unknown input rather than guessing
  PASS  Util.NormalizeClassToken upper-cases
  PASS  Util.DeepCopy clones nested tables (no shared refs)
  PASS  Util.Throttle coalesces a burst to one trailing-args call
  PASS  RegisterUnitCastEvent registers the dispatch frame for the named unit

test_coresetup.lua
  PASS  the harness loads the vendored LibKa0s majors, so the suite is not measuring a stub
  PASS  the runner's library load list matches libs/LibKa0s/LibKa0s.xml file for file
  PASS  every file the runner loads for LibKa0s exists on disk
  PASS  NS.SafeToString renders ordinary values through tostring
  PASS  NS.SafeToString answers nil and booleans up front, never masking them
  PASS  NS.SafeToString renders an unconcatable value as the shared <secret> sentinel
  PASS  NS.IsConcatSafe probes table.concat, not the .. operator
  PASS  NS.Util.print renders prefix, one space, then the body — byte for byte
  PASS  NS.Util.print space-joins its arguments, mirroring print()
  PASS  NS.Util.print is secret-safe: an unconcatable argument cannot raise
  PASS  NS.Util.print resolves the prefix at call time, not at load time
  PASS  NS.Util.print is the library printer, not a host reimplementation
  PASS  core/Util.lua no longer defines a printer of its own
  PASS  no addon file emits a bare "secret" sentinel of its own
  PASS  with LibKa0s absent the addon still loads and still prints tagged lines
  PASS  the degraded printer is still secret-safe and still says <secret>
  PASS  LibKa0s-Core-1.0 still has no user-visible strings to trap
  PASS  the Core descriptor passes no locale table, and the printer renders no key
  PASS  the shared cause clause is published on the healthy path too
  PASS  with LibKa0s absent all five seams say the same thing about WHY
  PASS  no seam re-spells the cause in its own words

test_util_anchor.lua
  PASS  SaveAnchor snapshots a frame's first anchor point
  PASS  SaveAnchor stores no frame reference, only serializable fields
  PASS  SaveAnchor falls back to a centered anchor for a nil frame
  PASS  SaveAnchor falls back to centered for a frame with no points set
  PASS  SaveAnchor reads point ONE, ignoring later anchors
  PASS  ApplyAnchor positions the frame against UIParent
  PASS  ApplyAnchor clears stale points instead of stacking them
  PASS  ApplyAnchor fills in centered defaults for a partial saved anchor
  PASS  ApplyAnchor is a no-op for a nil frame or nil anchor
  PASS  SaveAnchor and ApplyAnchor round-trip a dragged position exactly
  PASS  Throttle passes the LAST call's arguments, not the first
  PASS  Throttle re-arms after firing, so a later burst is not swallowed
  PASS  Throttle preserves embedded nils in the argument list
  PASS  Throttle survives a nil delay by treating it as immediate
  PASS  SpecDisplayName returns the client's LOCALIZED name for display
  PASS  SpecDisplayName returns the empty string for a non-number
  PASS  SpecDisplayName title-cases the English token for a spec the client can't name
  PASS  SpecDisplayName falls back to the raw ID for a spec nothing knows
  PASS  SpecTokenForID rejects a non-number rather than indexing with it
  PASS  SpecOrderForClass returns Blizzard's order, not numeric order
  PASS  SpecOrderForClass normalizes a lower-case class token
  PASS  SpecOrderForClass is nil for a class the client can't enumerate
  PASS  NormalizeClassToken upper-cases and tolerates nil
  PASS  RegisterUnitCastEvent forwards the event into the module's handler
  PASS  RegisterUnitCastEvent tolerates a handler that isn't defined yet
  PASS  RegisterUnitCastEvent returns a frame the caller can unregister

test_constants.lua
  PASS  Constants: the chat prefix is the cyan [KCD] tag and closes its color code
  PASS  Constants: the notice gray is an opener with no closer (callers add |r)
  PASS  Constants: the GCD upper bound covers an unhasted 1.5s global
  PASS  Constants: the cast bar's inside and outside insets are symmetric
  PASS  Constants: the panel header reserves more height than its top inset
  PASS  Constants: every panel metric is a positive number
  PASS  Constants: FONT_MONO points at a font that is actually shipped
  PASS  Constants: the shipped mono font ships its OFL license alongside it
  PASS  Constants: every spec ID is a positive integer
  PASS  Constants: every spec token is UPPER_SNAKE_CASE
  PASS  Constants: no two spec tokens share a spec ID
  PASS  Constants: the three Midnight-era spec IDs are present and correct
  PASS  Constants: SPEC covers all thirteen player classes at three specs each
  PASS  Constants: every spec ID has a reverse token
  PASS  Constants: the reverse map is exactly as large as the forward map
  PASS  Constants: the reverse map strips the disambiguating class suffix
  PASS  Constants: a non-shared token survives the suffix strip unchanged
  PASS  Constants: exactly the four reused spec names carry a class suffix
  PASS  Constants: every shared spec name is suffixed on every class that has it
  PASS  Constants: every spec key in defaults/Spells.lua is a known spec ID
  PASS  Constants: every spec ID in Const.SPEC has a shipped default list
  PASS  Constants: defaults ship one class table per class, all UPPER-case tokens

test_state.lua
  PASS  State: the combat flag starts false and holds `debug` session-only
  PASS  State.SetInCombat coerces any truthy value to a real boolean
  PASS  State: the bootstrap frame owns all three combat/login events
  PASS  State: PLAYER_REGEN_DISABLED / _ENABLED drive the flag both ways
  PASS  State: PLAYER_LOGIN seeds the flag from InCombatLockdown
  PASS  State: PLAYER_LOGIN releases its own registration after seeding
  PASS  State: every combat transition fans out COMBAT_STATE with the new flag
  PASS  IsHostileUnitCasting is false for a nil unit or one that doesn't exist
  PASS  IsHostileUnitCasting is false for a friendly caster
  PASS  IsHostileUnitCasting is true for a hostile CAST
  PASS  IsHostileUnitCasting is true for a hostile CHANNEL
  PASS  IsHostileUnitCasting is false for a hostile unit doing nothing
  PASS  IsHostileUnitCasting only truth-tests the cast name, never reads it
  PASS  IsHostileUnitCasting collapses the API multi-return to position 1
  PASS  ApplyInterruptibleAlpha refuses a frame that can't take the secret
  PASS  ApplyInterruptibleAlpha declines when the unit is absent or friendly
  PASS  ApplyInterruptibleAlpha declines when the unit has no cast at all
  PASS  ApplyInterruptibleAlpha maps interruptible -> alpha, uninterruptible -> 0
  PASS  ApplyInterruptibleAlpha defaults the visible alpha to 1
  PASS  ApplyInterruptibleAlpha passes a SECRET notInterruptible through verbatim
  PASS  ApplyInterruptibleAlpha reads the CHANNEL flag from position 7, not 8
  PASS  ApplyInterruptibleAlpha prefers the cast over a simultaneous channel
  PASS  ApplyInterruptibleAlpha never inspects the cast name it gates on

test_locale.lua
  PASS  frFR Elemental Shaman seeds a non-empty default spell list (issue #8)
  PASS  frFR and enUS Elemental Shaman seed byte-identical spell lists
  PASS  frFR player spec resolves to the locale-invariant numeric spec ID
  PASS  frFR client resolves a localized spec name typed at the slash command
  PASS  frFR Elemental Shaman actually watches its cooldowns end-to-end (issue #8)
  PASS  the Spells editor labels specs in the client's own language
  PASS  SpecDisplayName falls back to the English token for an unknown spec
  PASS  a spec-name lookup that ran before the client was ready retries later
  PASS  every default spell list is reachable on a French client

test_units.lua
  PASS  Units.LIST is target then focus
  PASS  target is never linked; focus honors its link flag
  PASS  Icons(focus) resolves to target's icons when linked
  PASS  IsEnabled combines master and per-unit enable
  PASS  CopyStyling snapshots target appearance into focus and unlinks
  PASS  Label.text is per-unit and not link-resolved
  PASS  IconGrid:ReconcileUnits enables focus once units.focus.enabled is true
  PASS  Castbar:ReconcileUnits mirrors IconGrid's enable/disable transitions
  PASS  master-enable off then on disables then revives both units without a reload
  PASS  LabelStyle resolves to target's style when focus is linked
  PASS  LabelShow follows the link: a linked focus mirrors target's show (spec 2b)
  PASS  CopyStyling snapshots target label.style + show, keeps focus text (spec 2a/2b)

test_schema.lua
  PASS  Settings.Schema is assembled from the settings/* files
  PASS  Helpers.ValidateSchema reports zero malformed rows
  PASS  Every schema row has a string path and a known type
  PASS  Helpers.Resolve walks a dotted path into db.profile
  PASS  icons/castbar/label schema rows are unit-scoped and valid
  PASS  Helpers.FindSchema locates a row by path
  PASS  General exposes focus rows; unit-selector panels still filter them out
  PASS  label panel carries per-unit label rows; General no longer does
  PASS  every label-panel row's default is a member of its static values list
  PASS  PartitionUnitRows splits alwaysPerUnit rows from styled rows
  PASS  debug console stays session-only: no schema row targets it (§12.5)

test_database.lua
  PASS  DEFAULT_PROFILE carries the expected top-level shape
  PASS  OnInitialize built a live db with a merged profile
  PASS  Schema version lives in db.global, not the profile (KCD-20)
  PASS  MigrateProfile is a no-op at the current schema version
  PASS  MigrateProfile treats a missing version as v1 and walks forward to current
  PASS  MigrateProfile adopts a legacy per-profile dbVersion even past AceDB backfill (KCD-20)
  PASS  GetSpellList returns nil for an unseeded class/spec
  PASS  MigrateSpecKeys rewrites an English spec-name key to its numeric spec ID
  PASS  MigrateSpecKeys rewrites a LOCALIZED spec-name key to its numeric spec ID (issue #8)
  PASS  MigrateSpecKeys is idempotent on an already-numeric profile
  PASS  MigrateSpecKeys leaves an unmappable key in place rather than dropping data
  PASS  MigrateSpecKeys does not clobber an existing numeric key on collision
  PASS  DEFAULT_PROFILE nests appearance under units.target / units.focus
  PASS  FoldLegacyUnits moves a legacy top-level config under units.target
  PASS  FoldLegacyUnits is idempotent and leaves a fresh v2 profile untouched
  PASS  DEFAULT_PROFILE ships an identical label.style for target and focus
  PASS  BackfillLabelStyle adds a missing label.style and preserves show/text
  PASS  BackfillLabelStyle is idempotent and leaves an existing style untouched
  PASS  BackfillLabelStyle key-fills a missing field onto an existing style, leaving other keys untouched
  PASS  DB label.style.color default matches the settings schema color row default (DB<->schema sync)

test_color_shape.lua
  PASS  the schema declares at least one color row per color-bearing panel
  PASS  every schema color default is keyed, never positional
  PASS  every schema color default carries all four channels
  PASS  every color row declares hasAlpha, so the picker keeps its alpha channel
  PASS  the built profile stores colors keyed
  PASS  DEFAULT_PROFILE and the schema agree on every color
  PASS  Util.Unpack reads the keyed shape
  PASS  Util.Unpack still reads a positional array, so a stray one renders rather than blanks
  PASS  no module reads a color by positional index any more
  PASS  a pre-migration profile's array colors convert to the keyed shape
  PASS  the migration bumps the stored schema version so it runs once
  PASS  an already-keyed color passes through the migration untouched
  PASS  the slash layer needs no color codec now the shapes agree
  PASS  set and get round-trip a color through the library with no translation
  PASS  every dropdown row's values is a keyed hash, never an array of records
  PASS  every static dropdown declares its order, so nothing silently alphabetizes
  PASS  the anchor dropdown still reads top row, bottom row, sides, center
  PASS  an LSM-backed row resolves its values at call time, never at declaration
  PASS  the valueGate hint explains WHY a gated dropdown value was rejected
  PASS  a rejected gated value carries the hint through the slash layer

test_bus.lua
  PASS  AceEvent mock fans one message out to two distinct targets
  PASS  Two receivers on the SAME target clobber (proves keying is by target)
  PASS  Addon SendMessage reaches a registered module target
  PASS  NewBusTarget gives each receiver its own target — both fire (KCD-09)

test_compat.lua
  PASS  Compat exposes the spec shims
  PASS  GetSpecialization prefers C_SpecializationInfo
  PASS  GetSpecializationInfo passes the multi-return through
  PASS  GetSpecialization falls back to the deprecated global when C_ is absent
  PASS  GetSpecializationInfo falls back to the deprecated global when C_ is absent

test_compat_api.lua
  PASS  Compat._firstReturn tolerates a nil API (pre-12.0 client)
  PASS  Compat._firstReturn collapses a multi-return to position 1
  PASS  Compat._firstReturn forwards its arguments to the API
  PASS  Compat._firstReturn returns a nil first value even when later ones are set
  PASS  GetSpellCooldown reads the modern C_Spell info table
  PASS  GetSpellCooldown returns the inert tuple when the API yields no info
  PASS  GetSpellCooldown treats a missing isEnabled as enabled, missing isActive as inactive
  PASS  GetSpellCooldown coerces a non-boolean isActive to false
  PASS  GetSpellCooldown passes SECRET timings through without touching them
  PASS  GetSpellCooldown falls back to the deprecated global on a pre-12.0 client
  PASS  GetSpellCooldown's legacy path reports a zero duration as off cooldown
  PASS  GetSpellCooldown's legacy path refuses to compare a secret duration
  PASS  GetSpellCooldown returns the inert tuple when NO cooldown API exists
  PASS  GetSpellCooldownDuration hands back the opaque handle unchanged
  PASS  GetSpellCooldownDuration is nil on a client without the 12.0 API
  PASS  GetSpellTexture prefers C_Spell and falls back to the global
  PASS  GetSpellTexture is nil when neither API exists
  PASS  GetSpellInfo flattens the modern info table into the legacy tuple order
  PASS  GetSpellInfo is nil for an unknown spell, without falling through
  PASS  GetSpellInfo falls back to the deprecated global's multi-return
  PASS  GetSpellCharges flattens the modern charge table
  PASS  GetSpellCharges is nil for a spell without charges
  PASS  GetSpellCharges passes a SECRET charge count through untouched
  PASS  GetSpellCharges falls back to the deprecated global
  PASS  IsSpellAvailable rejects a non-number spell ID outright
  PASS  IsSpellAvailable is true when IsPlayerSpell says so
  PASS  IsSpellAvailable falls back to the spellbook for racials and professions
  PASS  IsSpellAvailable catches PET spells via the IsSpellKnown pet flag
  PASS  IsSpellAvailable is false for an unpicked talent choice-node sibling
  PASS  IsSpellUsable reads the SpellUsabilityInfo table form
  PASS  IsSpellUsable reads the two-boolean form some Midnight builds return
  PASS  IsSpellUsable normalizes the legacy API's 1/nil into real booleans
  PASS  IsSpellUsable defaults to usable when no API is available
  PASS  GetCastingInfo builds a record from the CAST API's positions
  PASS  GetCastingInfo falls through to the channel shim when not casting
  PASS  GetCastingInfo is nil when the unit is neither casting nor channeling
  PASS  GetChannelInfo reads notInterruptible from position 7, spellID from 8
  PASS  GetChannelInfo is nil when the API itself is missing
  PASS  a FRIENDLY unit's cast is forced to uninterruptible regardless of the API
  PASS  a HOSTILE unit's raw notInterruptible flag is returned unchanged
  PASS  the friendly override applies to CHANNELS too, not just casts
  PASS  the cast record carries SECRET name/texture/flag/id without inspecting them
  PASS  the cast record attaches the plain-number CastingDuration object
  PASS  a channel record sources its duration from UnitChannelDuration
  PASS  a record survives a client with no duration API at all
  PASS  isChannel is a real boolean on both record paths

test_compat_debug.lua
  PASS  DebugInterrupt bails with the unit name when the unit does not exist
  PASS  DebugInterrupt defaults the unit to target
  PASS  DebugInterrupt's header quotes the unit name and reports canAttack
  PASS  DebugInterrupt renders a secret-tainted unit name as <secret>
  PASS  DebugInterrupt reports 'not casting' / 'not channeling' when nothing is cast
  PASS  DebugInterrupt dumps all nine UnitCastingInfo positions, in order
  PASS  DebugInterrupt dumps eight UnitChannelInfo positions — notInterruptible at 7
  PASS  DebugInterrupt renders a secret notInterruptible without touching tostring
  PASS  DebugInterrupt renders a nil position as the literal nil
  PASS  DebugInterrupt skips the casting block entirely when the API is absent
  PASS  DebugInterrupt closes with the addon's own visibility and glow decisions

test_debuglog.lua
  PASS  DebugLog module loaded with its public API
  PASS  FormatPlain is clean, un-colored, and well-shaped (§12.3)
  PASS  FormatColored carries the same fields as FormatPlain (no drift)
  PASS  debug flag defaults OFF and lives in State, never in SavedVariables (§12.5)
  PASS  SetEnabled is the single write seam and toggles State.debug
  PASS  SetEnabled brackets each session with a console line at both ends (§12.5)
  PASS  NS.Debug is a no-op when disabled (zero capture) and appends when enabled
  PASS  NS.Debug sanitizes secret args and never errors
  PASS  NS.Debug passes plain args through unchanged
  PASS  scrollbar + line-counter sync methods exist (§11)
  PASS  sync methods are a clean no-op before the window is built (§11)
  PASS  building the console + Add/Clear run the guarded sync headlessly (§11)
  PASS  console WINDOW visibility is decoupled from the capture flag (§12.5)

test_debuglogsetup.lua
  PASS  modules/DebugLog.lua has been deleted, not left beside the library
  PASS  the TOC lists core/DebugLogSetup.lua and no longer lists modules/DebugLog.lua
  PASS  FormatPlain renders <ts> | [<tag>] <msg> byte for byte
  PASS  FormatColored keeps the steel-blue stamp, tan tag and escaped pipe
  PASS  a nil tag renders as empty brackets rather than the string 'nil'
  PASS  the console registers under the same frame name modules/DebugLog.lua hardcoded
  PASS  the console title is the brand plus the library's own suffix
  PASS  the debug flag stays the addon's — the library never keeps a copy
  PASS  the enable ack renders the state word green, through the addon's tagged printer
  PASS  the disable ack renders the state word red
  PASS  enabling brackets the session and follows it with the host's [Init] summary
  PASS  NS.Debug is bound bare off the instance and takes no self
  PASS  NS.Debug is zero-cost when the flag is off
  PASS  a numeric format slot still renders correctly now the library stringifies every arg
  PASS  a secret argument renders as the shared sentinel and cannot raise
  PASS  with LibKa0s absent the stub answers every DebugLog member the addon calls
  PASS  the degraded stub still flips the flag and still prints the ack
  PASS  the degraded stub carries no copy of the line formatters
  PASS  every string the debug console renders resolves to prose, not to its own key
  PASS  the console title and checkbox carry prose, reached the way the UI reaches them
  PASS  the vendored DebugLog major falls THROUGH a key-returning locale table

test_icongrid_layout.lua
  PASS  Layout math is published on the IconGrid module
  PASS  IconGrid:Layout method survives alongside the geometry table
  PASS  parseAnchor normalizes modern, legacy, and CENTER tokens
  PASS  parseAnchor rejects invalid combos with the RIGHT/CENTER default
  PASS  parseGrow accepts perpendicular axes and defaults otherwise
  PASS  placeBlock RIGHT/CENTER geometry (primary left, block right, centered)
  PASS  placeBlock TOP/CENTER geometry (block above primary)
  PASS  placeBlock CENTER stacks both on the grid center

test_icongrid_apply.lua
  PASS  Icon:Apply skips glow work when no plain state field moved
  PASS  Icon:Apply STILL re-arms the swipe when only the handle changed
  PASS  Icon:Apply redoes glow work when `ready` actually flips
  PASS  Icon:Apply redoes glow work when the cooldown ends
  PASS  Icon:Apply forced re-apply redoes state work even when nothing moved
  PASS  Icon:Apply keeps the charges badge live when charges are secret

test_icongrid_visibility.lua
  PASS  the visibility deciders are published for testing
  PASS  visibilityMode reads the addon-wide setting
  PASS  visibilityMode defaults to 'always' when the field is missing
  PASS  the cast bar reads the SAME visibility setting as the grid
  PASS  master enable off hides the grid in every mode
  PASS  a fresh profile with no enable field reads as enabled
  PASS  unlocked shows the grid even in a mode that would hide it
  PASS  locked restores the mode's own decision
  PASS  'always' shows regardless of combat or casting
  PASS  an unrecognized mode falls back to always-visible
  PASS  'in_combat' follows State.inCombat in both directions
  PASS  'in_combat' ignores InCombatLockdown, which lags the regen events
  PASS  'target_casting' shows while the unit casts and hides when it stops
  PASS  'target_casting' counts a CHANNEL as casting
  PASS  'target_casting' hides when the unit doesn't exist
  PASS  'target_casting' does NOT filter on hostility, unlike the interruptible mode
  PASS  'target_casting_interruptible' shows for any HOSTILE cast
  PASS  'target_casting_interruptible' hides for a FRIENDLY cast
  PASS  'target_casting_interruptible' hides when nothing is being cast
  PASS  each unit's decision is made against its OWN unit token
  PASS  instanceCasting truth-tests the cast name without ever reading it
  PASS  instanceCasting is false for a unit that doesn't exist

test_icongrid_render.lua
  PASS  the render helpers are published for testing
  PASS  SafeUnpackColor reads both the array and hash color shapes
  PASS  SafeUnpackColor honors the caller's cooldown-tint fallback
  PASS  SafeUnpackColor falls back to opaque white with no fallback given
  PASS  UnpackGlowColor reads a configured array color
  PASS  UnpackGlowColor falls back to the shipped yellow glow for a non-table
  PASS  UnpackGlowColor fills missing channels rather than returning nils
  PASS  the 'always' trigger glows unconditionally
  PASS  the 'never' trigger and any unknown token keep the glow off
  PASS  'target_casting' asks the INSTANCE whether its unit is casting
  PASS  'target_casting' is false for an instance with no predicate yet
  PASS  'target_casting_interruptible' glows for ANY hostile cast
  PASS  'target_casting_interruptible' does NOT glow for a friendly cast
  PASS  'target_casting_interruptible' resolves per-unit, defaulting to target
  PASS  PlainStateMoved treats a first render as a change
  PASS  PlainStateMoved is false when nothing plain moved
  PASS  PlainStateMoved fires on a ready or isActive flip
  PASS  PlainStateMoved watches handle PRESENCE, not handle identity
  PASS  PlainStateMoved watches the charge timer's presence independently
  PASS  PlainStateMoved deliberately IGNORES charges, unlike the cooldown gates
  PASS  PlainStateMoved never compares a secret charge value

test_icongrid_curves.lua
  PASS  each unit gets its own curve pair
  PASS  an unlinked focus builds its curve from ITS OWN readyAlpha
  PASS  an unlinked focus builds its tint curve from ITS OWN cooldownTint
  PASS  a LINKED focus resolves to target's values
  PASS  CurvesFor never falls back to another unit's curves
  PASS  rebuilding with an unchanged config reuses the same curve objects
  PASS  an unrelated icons edit does NOT recreate the curves
  PASS  a readyAlpha edit DOES recreate the curve
  PASS  a cooldownAlpha edit DOES recreate the curve
  PASS  a cooldownTint edit DOES recreate the curve
  PASS  one unit's rebuild does not disturb the other's cached curves
  PASS  CurveSignature covers exactly the three curve-shaping fields

test_icongrid_curve_link.lua
  PASS  the mock's curve evaluation actually reads control points
  PASS  a LINKED focus renders with target's cooldown alpha
  PASS  unlinking focus via the `units` section re-renders with ITS OWN alpha
  PASS  re-linking focus via the `units` section restores target's alpha
  PASS  unlinking picks up focus's own cooldown TINT, not target's
  PASS  a per-unit enable toggle also refreshes curves

test_icongrid_buildlist.lua
  PASS  BuildActiveList renders one icon per enabled entry
  PASS  BuildActiveList preserves the saved list's ORDER
  PASS  the first entry becomes the primary icon the cast bar anchors to
  PASS  BuildActiveList replaces the previous list rather than appending to it
  PASS  an empty list produces an empty grid, not an error
  PASS  a disabled entry is skipped
  PASS  an entry with no enabled field is treated as enabled
  PASS  an entry with no spellID is skipped rather than acquiring a nil-keyed icon
  PASS  a spell the player cannot currently cast is not rendered
  PASS  a spell missing from the client's spell DB is not rendered
  PASS  a pet spell appears only while its pet is out
  PASS  a duplicate spellID is skipped, keeping the pool 1:1 with the ID
  PASS  the FIRST occurrence of a duplicated spellID is the one kept
  PASS  a duplicate that is DISABLED doesn't suppress the enabled original
  PASS  a duplicate is reported to the debug console when logging is on
  PASS  a SECRET icon texture is skipped instead of erroring the whole build
  PASS  a plain texture IS applied to the icon widget
  PASS  a class+spec with no saved list renders nothing and creates no entry
  PASS  BuildActiveList caches the unit's resolved icon config on the instance
  PASS  each unit builds from its own resolved config

test_icongrid_glowgate.lua
  PASS  RefreshAllGlows pushes the glow decision through every icon
  PASS  an unmoved gate short-circuits the per-icon loop
  PASS  an interruptibility flip moves the gate and re-iterates
  PASS  cast start and cast stop each move the gate
  PASS  the gate cache is per-instance, so target and focus don't clobber
  PASS  a SECRET interruptibility reading defeats the short-circuit
  PASS  the debug line dedups on the printed label
  PASS  each gate state gets its own debug label

test_lifecycle.lua
  PASS  addon + all modules enable cleanly on the Ace3 login path
  PASS  IconGrid:OnEnable installs its bus subscriptions
  PASS  Cooldowns and Castbar subscribe to CONFIG_CHANGED after enable
  PASS  post-enable CONFIG_CHANGED re-layout runs end-to-end without error

test_unitlabel.lua
  PASS  UnitLabel module is registered
  PASS  UnitLabel.ApplyAll runs without error for both units
  PASS  Castbar:GetCastbarFrame does not create an instance for an unknown unit
  PASS  UnitLabel:Apply parents the label to the icon grid, not the cast bar (General-visibility, not cast-gated)

test_unitlabel_apply.lua
  PASS  Apply writes the unit's own label text
  PASS  Apply writes an empty string rather than nil for a cleared label
  PASS  label TEXT stays per-unit even when the units are linked
  PASS  Apply pushes the configured size and color onto the FontString
  PASS  Apply always resolves a non-nil font path
  PASS  Apply falls back to a 14pt outline for a style with no size or flags
  PASS  Apply applies the configured horizontal justification
  PASS  Apply defaults justification to CENTER
  PASS  a LINKED focus renders target's styling but its own text
  PASS  Apply positions the label against its chosen ATTACH frame
  PASS  Apply parents the label to the ICON GRID even when attached to the cast bar
  PASS  Apply anchors POSITION to the cast bar while parenting to the grid
  PASS  re-applying never stacks anchors on the holder frame
  PASS  the label shows when the unit is enabled and show is on
  PASS  turning the label's show off hides it
  PASS  disabling the unit hides its label regardless of the show flag
  PASS  the master enable gates the label too
  PASS  a LINKED focus mirrors target's show flag (spec 2b)
  PASS  EnsureFrame builds the holder once and reuses it
  PASS  target and focus each get their own label widgets
  PASS  ApplyAll renders every unit in one pass

test_castbar.lua
  PASS  Castbar exposes the pure AutoSizeLong helper
  PASS  AutoSizeLong copies the grid extent verbatim when scales match
  PASS  AutoSizeLong shrinks the bar when the grid is scaled down (master scale < 1)
  PASS  AutoSizeLong grows the bar when the grid is scaled up (master scale > 1)
  PASS  AutoSizeLong honors the bar's own effective scale
  PASS  AutoSizeLong returns the fallback for a zero/nil grid extent
  PASS  AutoSizeLong treats a zero/nil scale as 1 (never divides by zero)

test_castbar_helpers.lua
  PASS  the Castbar pure helpers are published for testing
  PASS  UnpackColor reads an array-style color
  PASS  UnpackColor reads a hash-style color
  PASS  UnpackColor uses the CALLER's fallback for a nil color
  PASS  UnpackColor falls back to opaque white when the caller gives no fallback
  PASS  UnpackColor defaults a missing alpha to fully opaque
  PASS  TruncateName leaves a name shorter than the cap alone
  PASS  TruncateName leaves a name EXACTLY at the cap alone
  PASS  TruncateName clips and appends an ellipsis past the cap
  PASS  TruncateName treats 0 and nil as 'no truncation'
  PASS  TruncateName treats a negative cap as 'no truncation'
  PASS  TruncateName returns an empty string for a nil name
  PASS  TruncateName passes a SECRET name through without measuring it
  PASS  StateConfig returns the configured per-state table when present
  PASS  StateConfig falls back when the state key is missing
  PASS  StateConfig rejects a non-table value stored under the state key
  PASS  the interruptible fallback is gold with no border, the uninterruptible red with one
  PASS  both state fallbacks carry every field the reskin path reads
  PASS  ToSetPoint maps every schema anchor token to a real SetPoint token
  PASS  ToSetPoint defaults a nil anchor to CENTER
  PASS  ToSetPoint passes an already-valid SetPoint token straight through
  PASS  FetchStatusBarTexture returns the LSM path when the key resolves
  PASS  FetchStatusBarTexture degrades to a client-shipped path for an unknown key
  PASS  FetchBorderTexture degrades to a client-shipped border
  PASS  FetchFont always yields a usable font path
  PASS  AutoSizeLong matches on-screen extents for frames at different scales
  PASS  AutoSizeLong accounts for scale INHERITED from a parent frame

test_castbar_frame.lua
  PASS  EnsureFrame builds the full widget stack once and reuses it
  PASS  EnsureFrame creates BOTH state bars and both backgrounds
  PASS  EnsureFrame parents the state bars inside the bar container
  PASS  EnsureFrame seeds both bars to an empty 0..1 range
  PASS  target and focus get separate frames, not one shared bar
  PASS  GetCastbarFrame never creates an instance for an unknown unit
  PASS  Start renders the cast name into the bar's FontString
  PASS  Start applies the user's name truncation
  PASS  Start blanks the name entirely when showName is off
  PASS  Start pushes the cast's icon texture onto the icon widget
  PASS  a CAST fills the bar 0 -> total
  PASS  a CHANNEL drains the bar total -> 0
  PASS  both stacked bars carry identical values so the alpha switch is seamless
  PASS  Start shows the frame and arms the per-frame OnUpdate
  PASS  Start refuses a record with no duration object rather than faking one
  PASS  the OnUpdate tick advances the bar as the cast progresses
  PASS  the OnUpdate tick writes the remaining/total countdown when showTime is on
  PASS  the OnUpdate tick leaves the countdown alone when showTime is off
  PASS  the OnUpdate script disarms itself once the cast record is gone
  PASS  an INTERRUPTIBLE cast shows the interruptible widgets and hides the others
  PASS  an UNINTERRUPTIBLE cast flips the whole stack the other way
  PASS  the alpha switch never branches on notInterruptible in Lua
  PASS  the no-cast state falls back to interruptible visuals
  PASS  the uninterruptible warning border is on and the interruptible one off
  PASS  Stop clears the cast, empties both bars and disarms the animation
  PASS  Stop HIDES the bar while locked
  PASS  Stop leaves a PREVIEW on screen while unlocked, so it stays draggable
  PASS  the preview resets the bar range so it doesn't inherit the last cast's total
  PASS  Stop is safe before the frame has ever been built
  PASS  a cast starting out of combat is suppressed in the in_combat mode
  PASS  the same cast shows once combat is flagged
  PASS  disabling the cast bar for a unit keeps its frame hidden through a cast
  PASS  the interruptible mode masks the bar's alpha from the SECRET flag
  PASS  an interruptible cast in that mode stays fully visible
  PASS  ApplyAnchor in FREE mode restores the saved anchor against UIParent
  PASS  re-anchoring never stacks a second point on the frame

test_castbar_skin.lua
  PASS  StructureSignature is stable for identical inputs
  PASS  StructureSignature moves when a structural field moves
  PASS  StructureSignature moves when the RESOLVED size moves
  PASS  StructureSignature ignores pure color fields
  PASS  StructureSignature DOES move for border size/texture
  PASS  Reskin stamps a structure signature on the instance
  PASS  a structural config change re-sizes the frame
  PASS  a SECOND structural change still lands (the guard is not one-shot)
  PASS  re-skinning with no config change leaves the signature untouched
  PASS  a color-only change does NOT move the structure signature
  PASS  a color-only change still repaints the bar
  PASS  force rebuilds the geometry even when the signature matches
  PASS  Reskin is safe before the frame has ever been built
  PASS  target and focus carry independent structure signatures
  PASS  ResolveBarSize floors the long and thick axes
  PASS  ResolveBarSize returns the configured size when auto-size is off
  PASS  ResolveBarSize leaves thickness alone in vertical orientation
  PASS  HORIZONTAL + iconPosition LEFT insets the bar from the left
  PASS  HORIZONTAL + iconPosition RIGHT insets the bar from the right
  PASS  VERTICAL remaps iconPosition LEFT to the TOP of the bar
  PASS  VERTICAL remaps iconPosition RIGHT to the BOTTOM of the bar
  PASS  iconPosition OFF hides the icon and gives the bar the whole frame
  PASS  a zero iconSize hides the icon as surely as OFF does
  PASS  iconSize is clamped to the bar's thickness
  PASS  HORIZONTAL grow RIGHT puts the spark on the bar's RIGHT fill edge
  PASS  HORIZONTAL grow LEFT reverses the fill and the spark rides LEFT
  PASS  VERTICAL grow UP puts the spark on the TOP fill edge
  PASS  VERTICAL grow DOWN reverses the fill and the spark rides BOTTOM
  PASS  the spark is sized across the bar and rotated 90 degrees when vertical
  PASS  the spark is unrotated and tall when horizontal
  PASS  showSpark=false hides the spark outright
  PASS  showSpark defaults to shown when unset
  PASS  both labels share the config font, and NONE flags normalize to empty
  PASS  absent fontFlags normalize to empty too
  PASS  showName / showTime false hide their labels, and only theirs
  PASS  the labels default to shown when the flags are unset
  PASS  name and time labels anchor per their independent position settings
  PASS  Reskin survived the peel as a method on the Castbar module
  PASS  the skin sibling reads its helpers off the module, not a private copy
  PASS  modules/Castbar.lua sits under the 1500-LOC hard cap (layout-§1)

test_castbar_debug.lua
  PASS  DebugDump opens with the resolved unit and bails when it does not exist
  PASS  DebugDump defaults the unit to target
  PASS  DebugDump's unit line reports name, isUnit and canAttack
  PASS  DebugDump reports isUnit=self for the player's own unit
  PASS  DebugDump falls back to '?' when the unit has no name
  PASS  DebugDump stops after the no-cast line when nothing is tracked
  PASS  DebugDump flags a missed event when Compat still sees a cast
  PASS  DebugDump reports a plain boolean notInterruptible by value
  PASS  DebugDump reports a nil notInterruptible as interruptible
  PASS  DebugDump reports a secret notInterruptible without touching tostring
  PASS  DebugDump prints no state line for a secret value with no curve evaluator
  PASS  DebugDump reports the channel flag and the record's field TYPES only
  PASS  DebugDump reports secret record fields by type, never by value
  PASS  DebugDump renders the configured per-state colors from the live profile
  PASS  DebugDump reads colors through Util.Unpack, in the keyed storage shape
  PASS  DebugDump reports a missing color table as (missing)
  PASS  DebugDump reports the colors live on the StatusBar widgets
  PASS  DebugDump says (no widget) before the frame has ever been built

test_cooldowns.lua
  PASS  SPELL_UPDATE_* burst coalesces to one Refresh per frame
  PASS  Refresh logs one coalesced line only when a spell changed
  PASS  Refresh coalesces multiple simultaneous changes into ONE line
  PASS  Refresh does not log when only the cooldown handle identity changed
  PASS  Refresh STILL emits SPELL_STATE when the cooldown handle changed
  PASS  Refresh logs a genuine on-cooldown -> ready transition
  PASS  Rebuild summary names the class/spec IDs and every watched + skipped spell
  PASS  Rebuild summary distinguishes an empty watched set from an empty skipped set
  PASS  Rebuild summary re-logs when only the SKIPPED set changes
  PASS  Rebuild summary logs on a material change and is silent on a repeat
  PASS  Refresh logs nothing when no spell changed

test_cooldowns_gates.lua
  PASS  both gates are published for testing
  PASS  both gates treat a missing previous state as a change (first poll)
  PASS  both gates are silent when nothing at all moved
  PASS  both gates fire on a ready flip — the transition that matters most
  PASS  both gates fire on an isActive flip
  PASS  both gates fire when a cooldown handle APPEARS
  PASS  both gates fire when a cooldown handle DISAPPEARS
  PASS  both gates fire when a charge-recharge timer appears
  PASS  StateChanged fires on a NEW handle for the same cooldown
  PASS  MaterialChange ignores a new handle for the same cooldown
  PASS  MaterialChange ignores a new CHARGE handle too
  PASS  both gates ignore charges that stayed nil
  PASS  both gates fire on a plain charge count that moved
  PASS  both gates ignore a plain charge count that held still
  PASS  both gates fire when charges appear from nothing
  PASS  StateChanged EMITS conservatively when either charge count is secret
  PASS  MaterialChange stays QUIET when either charge count is secret
  PASS  one secret side is enough to trigger each gate's secret rule
  PASS  a secret charge count never blocks a real ready/isActive transition
  PASS  neither gate ever reads a secret charge value itself
  PASS  Cooldowns.MasterEnabled defaults to true when the field is absent
  PASS  Cooldowns.MasterEnabled is false only for an explicit false

test_settings_log.lua
  PASS  Helpers.Set logs one debounced [Set] line with the settled value
  PASS  Helpers.Set formats an RGBA table compactly
  PASS  ResetIconPosition restores units.target.anchors.icons to the default (Task 8 fix)
  PASS  ResetAll (via ResetAllPositions) restores both units' icons+castbar anchors to default (resetall bug fix)
  PASS  ResetAll (via RestoreUnitLinks) restores each unit's link flag to default (link reset bug fix)

test_settings_spells.lua
  PASS  Spells editor seeds its selection to the player's own class and spec
  PASS  Spells editor selection follows an in-game spec change (dropdown regression)
  PASS  Spells editor spec change also tracks a class it can render
  PASS  Spells editor exposes specs in Blizzard's order, not numeric order

test_settings_spells_editor.lua
  PASS  the Add-spell popup appends a validated spell to the selected list
  PASS  input the spell DB does not resolve is refused and nothing is added
  PASS  re-adding a spell already in the list re-enables it in place
  PASS  adding to a spec the user has never customized lazy-creates its list
  PASS  a spell the Cooldown Manager does not track for the player's own spec is refused
  PASS  a spell the Cooldown Manager does track passes the gate
  PASS  the gate is DROPPED when the editor is not on the player's live spec
  PASS  an absent C_CooldownViewer falls through leniently rather than refusing
  PASS  an API that answers nothing is remembered as empty and never re-walked
  PASS  a category the client throws on is survived rather than aborting the walk
  PASS  a spell row carries its nine widgets in the visual column order
  PASS  the row's status glyph reflects Compat.IsSpellAvailable and does not gate the row
  PASS  the row checkbox writes the entry's enabled flag as a real boolean
  PASS  a disabled row renders its spell icon and checkbox from the stored flag
  PASS  the move buttons are disabled at the list edges, not merely inert
  PASS  Move up swaps the entry with the one above it
  PASS  Move down swaps the entry with the one below it
  PASS  Remove deletes exactly the row's entry
  PASS  the category dropdown writes the entry's category
  PASS  RefreshRows builds the header, the scroll container and one row per entry
  PASS  an empty list renders the guidance label instead of rows
  PASS  RefreshRows refuses to run against a hidden panel
  PASS  a rebuild releases the previous widget tree before building a new one
  PASS  the selection cascade falls back to the first sorted class the defaults know
  PASS  a stale move click after a rebuild cannot run off the end of the list

test_settings_widgets.lua
  PASS  the settings helpers are published for testing
  PASS  SortedKeys returns a deterministic ordering
  PASS  SortedKeys returns an empty list for a nil or non-table input
  PASS  SpecOrder lists specs in Blizzard's order, not numeric order
  PASS  SpecOrder falls back to sorted numeric keys when the client can't be queried
  PASS  SpecOrder only offers specs the addon ships defaults for
  PASS  SpecOrder lists EVERY spec the defaults ship for that class
  PASS  SpecOrder is empty for a class with no shipped defaults
  PASS  Druid's four specs all survive the ordering
  PASS  ValidateSpellInput accepts a numeric spell ID and resolves its name
  PASS  ValidateSpellInput accepts an ID typed as a string
  PASS  ValidateSpellInput rejects empty and nil input
  PASS  ValidateSpellInput rejects an ID the client doesn't know
  PASS  ValidateSpellInput resolves a spell NAME to its numeric ID
  PASS  ValidateSpellInput rejects a name that resolves to no spell
  PASS  ValidateSpellInput refuses a name whose lookup yields no ID
  PASS  ClassDisplayName spaces the two-word class tokens
  PASS  TitleCaseToken lower-cases everything after the first letter
  PASS  TitleCaseToken returns an empty string for nil rather than erroring
  PASS  every shipped class token produces a non-empty display name

test_options_panel.lua
  PASS  the canvas frame carries OnCommit, OnDefault and OnRefresh from the library
  PASS  OnDefault reaches a defaultsOnClick parked AFTER the panel is built
  PASS  a page that parks no defaults action still has a callable, inert OnDefault
  PASS  NS.Settings.Helpers IS the library instance, decorated in place
  PASS  the host ships no widget maker, flow engine or layout constant of its own
  PASS  a bool row renders a checkbox labeled from the row
  PASS  a number row renders a slider carrying the row's range
  PASS  a string row renders a dropdown listing the KEYED options in declared order
  PASS  a color row renders a picker with alpha and the decoded color
  PASS  ticking a checkbox writes through the addon's single write seam
  PASS  a checkbox write fires CONFIG_CHANGED with the row's section
  PASS  dragging a slider commits on mouse-up
  PASS  choosing a dropdown option stores the option KEY, never its index
  PASS  confirming a color stores the keyed shape the modules read
  PASS  an external write re-syncs an open widget through its refresher
  PASS  releasing a page's widgets drops that page's refreshers
  PASS  InlinePair puts both caller-supplied widgets in ONE row
  PASS  SessionToggle adapts this addon's argument order onto the library's
  PASS  a session toggle never becomes a saved setting
  PASS  the Profiles page is vetoed from a global reset
  PASS  a global reset also clears the state no schema row owns
  PASS  with LibKa0s absent the schema still loads COMPLETE
  PASS  the degraded stub keeps the global reset real
  PASS  the degraded stub opens no panel and says so once
  PASS  the degraded stub carries no widget maker or layout constant
  PASS  every schema row the panel renders is labeled with prose, not with a key
  PASS  the panel's group and section headings are prose too
  PASS  libs/LibKa0s/Options.lua takes no locale override, so none can be mis-passed

test_settings_refreshers.lua
  PASS  rendering rows registers refreshers
  PASS  ClearScroll empties the refresher registry
  PASS  a clear-and-rebuild cycle does not grow the registry
  PASS  RefreshAllPanels never runs a refresher from a cleared render
  PASS  ClearScroll is safe on a ctx that never rendered

test_flow_traces.lua
  PASS  OnProfileChanged logs a [Profile] line

test_version.lua
  PASS  `version` is a registered COMMANDS verb (slash-commands-§3)
  PASS  `/kcd version` prints v<version> on exactly one line
  PASS  `version` falls back to the NS.VERSION stamp when TOC metadata is absent

test_slash_style.lua
  PASS  /kcd help emits no line ending in ':' (slash-commands-§4)
  PASS  bare /kcd emits no line ending in ':'
  PASS  /kcd debug sub-header emits no line ending in ':'
  PASS  /kcd spells sub-header emits no line ending in ':'
  PASS  every COMMANDS verb description is free of a trailing ':'
  PASS  an unknown verb's error line does not end in ':'
  PASS  /kcd debug spells emits no line ending in ':'
  PASS  /kcd debug castbar emits no line ending in ':'
  PASS  /kcd debug interrupt emits no line ending in ':'
  PASS  no addon source passes a ':'-terminated literal to a printer

test_slash.lua
  PASS  the dispatcher instance is built from LibKa0s-Slash-1.0
  PASS  NS.COMMANDS stays the host's, as ordered positional triples
  PASS  every COMMANDS handler takes (rest), not (self, rest)
  PASS  an unknown verb names it and then prints the help index
  PASS  only the verb is lowercased — a schema path keeps its case
  PASS  the `options` alias still reaches `config`
  PASS  the help header now carries the em dash the standard mandates
  PASS  a help row is the one shared formatter, two-space indented
  PASS  the landing page renders the SAME rows, un-indented
  PASS  the panel no longer carries a second command-row formatter
  PASS  list groups by the row's panel, in the addon's declared page order
  PASS  get echoes the shared key = value pair
  PASS  set clamps out of range and echoes what was actually STORED
  PASS  set routes through the host's single write seam
  PASS  a color round-trips through the library with no host translation
  PASS  a color given in 0-255 rescales jointly
  PASS  an unknown path says so rather than writing anything
  PASS  reset takes a PATH and resets exactly that one row
  PASS  the old page-shaped reset names its replacement instead of going quiet
  PASS  `reset spells` names the verb its database rebuild moved to
  PASS  the spell-database rebuild survives, under its new verb
  PASS  resetall keeps its four-part host semantics rather than becoming CliResetAll
  PASS  with LibKa0s absent /kcd still answers and host verbs still work
  PASS  the degraded stub carries no copy of the row formatter or the parser
  PASS  every string the Slash CLI renders resolves to prose, not to its own key
  PASS  no chrome line /kcd prints is a raw SCREAMING_SNAKE key
  PASS  the vendored Slash major falls THROUGH a key-returning locale table

test_opensettings.lua
  PASS  OpenSettings refuses in combat and clears the retry counter
  PASS  OpenSettings also reads combat off InCombatLockdown
  PASS  OpenSettings opens the registered category and clears the retry counter
  PASS  OpenSettings defers with a notice when the Settings layer has not registered
  PASS  OpenSettings retries are bounded, then fall through to the plain notice
  PASS  OpenSettings prints the plain notice when the Settings API itself is absent
  PASS  OpenSettings' deferred retry re-enters the same function

test_perfsetup.lua
  PASS  NS.Perf is the library instance, with the hot-path gate as a plain field
  PASS  the capture ring is declared in the TOC as a second SavedVariables global
  PASS  every bracket call site reads the gate through a load-time upvalue
  PASS  every declared bucket is reached by a real bracket
  PASS  the declared bucket list and the bracketed call sites agree exactly
  PASS  nesting is declared for every bucket that runs inside another
  PASS  instrumentation is inert when capture is off
  PASS  the show decisions consult Perf.suspended as step 0, at the source
  PASS  suspend releases the per-unit dispatch frames AceEvent cannot reach
  PASS  enabling a unit while suspended does not re-register its frames mid-capture
  PASS  resume restores from CURRENT state, not from a snapshot
  PASS  the suspended flag is session-only and never persisted
  PASS  `perf` is a host verb in NS.COMMANDS, not registered by the library
  PASS  a bare /kcd perf answers through the addon's tagged printer
  PASS  with LibKa0s absent the probe stub answers every member the addon calls
  PASS  with LibKa0s absent the bracketed paths still run
  PASS  the perf panel resolves real English, never a raw STRINGS key
  PASS  no LibKa0s descriptor is handed the key-returning locale table
  PASS  the panel title is the host's brand plus the library's resolved suffix
  PASS  the VENDORED library ignores a fallback-synthesized locale entry
  PASS  the record stamps a real addon version, never "?"
  PASS  the perf version agrees with the one /kcd version prints
  PASS  the version fallback is reachable, not dead
  PASS  every PollSpell exit is measured, including the rejections
  PASS  the record stamps a real client interface version, never 0

test_list_mode.lua
  PASS  --list emits a generated '# Test Cases' inventory header + regen note
  PASS  --list stdout is inventory-only, no run output
  PASS  --list emits CRLF line endings (matches the repo eol=crlf policy)
  PASS  --list per-suite header counts match their bullet counts
  PASS  --list Totals row equals the grand total of bullets

test_vendor_sync.lua
  PASS  libs/LibKa0s is the LibKa0s release the README says this addon bundles
  PASS  tests/_kit is the test kit that shipped with that release

-------------------
737 passed, 0 failed
